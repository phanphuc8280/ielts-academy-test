# Ask Chị Context Pack 19
## Student Profile Signals
- Rule 19.1: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Lesson Progress Signals
- Rule 19.1: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Homework Signals
- Rule 19.1: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Error Log Signals
- Rule 19.1: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Teacher Notes Signals
- Rule 19.1: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Prompt Level Rules
- Rule 19.1: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Rewrite Decision Rules
- Rule 19.1: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.2: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.3: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.4: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.5: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.6: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.7: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.8: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.9: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.10: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.11: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.12: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.13: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.14: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 19.15: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
