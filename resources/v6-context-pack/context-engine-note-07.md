# Ask Chị Context Pack 07
## Student Profile Signals
- Rule 7.1: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `student_profile_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Lesson Progress Signals
- Rule 7.1: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `lesson_progress_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Homework Signals
- Rule 7.1: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `homework_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Error Log Signals
- Rule 7.1: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `error_log_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Teacher Notes Signals
- Rule 7.1: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `teacher_notes_signals` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Prompt Level Rules
- Rule 7.1: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `prompt_level_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
## Rewrite Decision Rules
- Rule 7.1: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.2: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.3: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.4: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.5: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.6: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.7: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.8: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.9: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.10: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.11: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.12: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.13: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.14: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
- Rule 7.15: Khi dữ liệu `rewrite_decision_rules` thay đổi, prompt Ask Chị phải tự ghi lại context mới nhất để giáo viên hiểu học viên đang ở đâu, đang kẹt chỗ nào và cần feedback đúng skill.
