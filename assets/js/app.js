(function(){
const pages=[
 ['dashboard','Learning Console','⌘','dashboard.html',['student','teacher','admin'],'Console'],
 ['roadmap','Progress Pipeline','🗺️','roadmap.html',['student','teacher','admin'],'Learn'],
 ['chapter-review','Chapter Review','🧭','chapter-review.html',['student','teacher','admin'],'Learn'],
 ['lesson','Lesson Classroom','📘','lesson.html',['student','teacher','admin'],'Learn'],
 ['exercise-bank','Exercise Bank','🧪','exercise-bank.html',['student','teacher','admin'],'Learn'],
 ['practice','Skill Practice','⚡','practice.html',['student','teacher','admin'],'Learn'],
 ['homework','Homework','📚','homework.html',['student','teacher','admin'],'Learn'],
 ['essays','Essay Workspace','📝','essays.html',['student','teacher','admin'],'Learn'],
 ['prompt-bank','Prompt Bank','🧠','prompt-bank.html',['student','teacher','admin'],'Learn'],
 ['sample-database','Sample Database','🏆','sample-database.html',['student','teacher','admin'],'Learn'],
 ['error-book','Error Log','🐞','error-book.html',['student','teacher','admin'],'Learn'],
 ['review-queue','Review Queue','🔁','review-queue.html',['student','teacher','admin'],'Learn'],
 ['vocabulary','Vocabulary','💬','vocabulary.html',['student','teacher','admin'],'Learn'],
 ['idea-bank','Idea Bank','💡','idea-bank.html',['student','teacher','admin'],'Learn'],
 ['band-tracker','Band Tracker','📈','band-tracker.html',['student','teacher','admin'],'Learn'],
 ['ai-context-builder','AI Context Builder','🤖','ai-context-builder.html',['student','teacher','admin'],'Tutor Tools'],
 ['course-qa','Course QA Mode','⭐','course-qa.html',['student','teacher','admin'],'Tutor Tools'],
 ['study-buddy','Study Buddy','🤝','study-buddy.html',['student','teacher','admin'],'Tutor Tools'],
 ['teacher-notes','Teacher Notes','🗒️','teacher-notes.html',['student','teacher','admin'],'Tutor Tools'],
 ['feedback','Feedback','🧾','feedback.html',['student','teacher','admin'],'Tutor Tools'],
 ['profile-backup','Profile & Backup','🧰','profile-backup.html',['student','teacher','admin'],'Account'],
 ['teacher-dashboard','Teacher Dashboard','🧑‍🏫','teacher-dashboard.html',['teacher','admin'],'Teacher'],
 ['teacher-mode','Teacher Mode','👩‍🏫','teacher-mode.html',['teacher','admin'],'Teacher'],
 ['custom-assignment','Custom Assignment','🧩','custom-assignment.html',['teacher','admin'],'Teacher'],
 ['admin-dashboard','Admin Dashboard','🛡️','admin-dashboard.html',['admin'],'Admin'],
 ['user-management','User Management','👥','user-management.html',['admin'],'Admin'],
 ['course-builder','Course Builder','🏗️','course-builder.html',['admin'],'Admin'],
 ['changelog','Changelog','🚀','changelog.html',['student','teacher','admin'],'System'],
 ['login','Login','🔐','login.html',['student','teacher','admin'],'Account']
]
const tabKeys=[['overview','Overview'],['theory','Theory'],['examples','Examples'],['analysis','Guided Analysis'],['skill','Skill Practice'],['homework','Homework'],['feedback','AI Feedback'],['rewrite','Rewrite'],['reflection','Reflection'],['mastered','Mastered']];
function inPagesDir(){return /(?:^|\/)pages(?:\/|$)/.test(location.pathname)}
function base(){return inPagesDir()?'':'pages/'}
function pageHref(file){return base()+file}
function pageFromUrl(){const file=(location.pathname.split('/').pop()||'dashboard.html').replace(/\.html$/,'');const found=pages.find(p=>p[3]===file+'.html'||p[0]===file);return found?found[0]:(document.body.dataset.page||'dashboard')}
function title(t,s){const h=document.getElementById('pageTitle'), sub=document.getElementById('pageSub'); if(h)h.textContent=t; if(sub)sub.textContent=s||''}
function navLink(p,page){return `<a class="nav-link ${p[0]===page?'active':''}" href="${pageHref(p[3])}" data-route="${p[0]}" data-file="${p[3]}"><span>${p[2]}</span><span>${p[1]}</span></a>`}
function layout(page){const data=Store.load();document.documentElement.dataset.theme=data.settings.theme||'dark';document.body.dataset.theme=data.settings.theme||'dark';document.body.dataset.page=page;const app=document.getElementById('app');app.innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-logo">IA</div><div><h1>IELTS Academy</h1><p>Compile your essay skills</p></div></div><div class="nav-section">Console</div>${pages.slice(0,1).map(p=>navLink(p,page)).join('')}<div class="nav-section">Build</div>${pages.slice(1,10).map(p=>navLink(p,page)).join('')}<div class="nav-section">Tutor Tools</div>${pages.slice(10).map(p=>navLink(p,page)).join('')}<div class="sidebar-mini"><strong>Progress pipeline</strong><p>Learn → Skill Practice → Homework → Teacher Feedback → Rewrite → Review → Mastered.</p><div class="progress"><span style="width:${Tutor.progress()}%"></span></div><p>${Tutor.progress()}% compiled · ${data.xp||0} XP · ${Tutor.masteryLevel()}</p></div></aside><main class="main"><div class="topbar"><div><h2 id="pageTitle"></h2><p id="pageSub"></p></div><div class="top-actions"><input class="search" id="globalSearch" placeholder="Search prompts, lessons, errors..."/><button class="btn ghost" id="themeBtn">🌗 Theme</button><a class="btn primary glow" href="${pageHref('dashboard.html')}" data-route="dashboard" data-file="dashboard.html">Run Today’s Lesson →</a></div></div><div id="view" class="page-transition"></div><div class="footer-note">IELTS Academy v4.0 chạy offline bằng localStorage. Dữ liệu chỉ nằm trong trình duyệt này. Migration giữ dữ liệu v2.x.</div></main></div>`;document.getElementById('themeBtn').onclick=()=>{Store.update(d=>{d.settings.theme=d.settings.theme==='dark'?'light':'dark';return d});renderPage(page)};document.getElementById('globalSearch').addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.value.trim())navigate('prompt-bank',`prompt-bank.html?q=${encodeURIComponent(e.target.value.trim())}`)});document.querySelectorAll('[data-route]').forEach(a=>a.addEventListener('click',e=>{if(e.button||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();navigate(a.dataset.route,a.getAttribute('href'))}))}
function mapRenderers(){return {dashboard:renderDashboard,roadmap:renderRoadmap,'chapter-review':renderChapterReview,lesson:renderLesson,'exercise-bank':renderExerciseBank,practice:renderPractice,homework:renderHomework,essays:renderEssays,'prompt-bank':renderPromptBank,'sample-database':renderSampleDatabase,'error-book':renderErrorLog,'review-queue':renderReviewQueue,vocabulary:renderVocabulary,'idea-bank':renderIdeaBank,'band-tracker':renderBandTracker,'teacher-mode':renderTeacherMode,'custom-assignment':renderCustomAssignment,'study-buddy':renderStudyBuddy,'teacher-notes':renderTeacherNotes,feedback:renderFeedback,'profile-backup':renderProfileBackup,changelog:renderChangelog,settings:renderProfileBackup,onboarding:renderOnboarding}}
function renderError(page,err){console.error(err);const v=document.getElementById('view');if(v)v.innerHTML=`<section class="card danger-box"><h3>Trang này gặp lỗi render</h3><p>Sidebar vẫn hoạt động. Hãy về Dashboard hoặc báo mình dòng lỗi này.</p><pre>${UI.esc(err&&err.message?err.message:String(err))}</pre><a class="btn primary" href="${pageHref('dashboard.html')}" data-route="dashboard">Về Dashboard</a></section>`}
function renderPage(page){Store.touchStudy();Tutor.runAdaptiveRules();if(page==='onboarding'){renderOnboarding();return}layout(page);try{(mapRenderers()[page]||renderDashboard)()}catch(err){renderError(page,err)}}
function navigate(page,href){try{history.pushState({page},'',href||pageHref((pages.find(p=>p[0]===page)||pages[0])[3]))}catch(e){}renderPage(page);window.scrollTo({top:0,behavior:'smooth'})}
function init(){renderPage(pageFromUrl());const d=Store.load();if(!d.studentProfile.onboarded && pageFromUrl()==='dashboard'){const v=document.getElementById('view');v.insertAdjacentHTML('afterbegin',`<section class="card warning-box"><h3>Setup profile để cá nhân hóa Learning Console</h3><p>Em vẫn dùng web được, nhưng profile giúp web biết ngày thi, mục tiêu và plan hôm nay.</p><div class="mini-actions"><a class="btn primary" href="${pageHref('onboarding.html')}" data-route="onboarding">Setup Profile</a><button class="btn" id="skipSetup">Dùng tạm profile mặc định</button></div></section>`);document.getElementById('skipSetup').onclick=()=>{Store.update(x=>{x.studentProfile.onboarded=true;if(!x.studentProfile.name)x.studentProfile.name='Học viên IELTS';return x});renderPage('dashboard')}}}
window.addEventListener('popstate',()=>renderPage(pageFromUrl()));window.addEventListener('DOMContentLoaded',init);

function dateDiff(date){if(!date)return null;return Math.ceil((new Date(date+'T23:59:59')-new Date())/86400000)}
function currentStatus(id,d=Store.load()){return Tutor.lessonLockStatus(id,d)}
function statusLabel(s){return s==='practice'?'skill practice':s}
function nextLesson(d=Store.load()){return IELTS_DATA.lessons.find(l=>currentStatus(l.id,d)!=='mastered'&&currentStatus(l.id,d)!=='locked')||IELTS_DATA.lessons.find(l=>currentStatus(l.id,d)==='locked')||IELTS_DATA.lessons[0]}
function dueReviews(d=Store.load()){const today=Store.today();return (d.reviewQueue||[]).filter(q=>q.status!=='done'&&(!q.nextReviewDate||q.nextReviewDate<=today)).sort((a,b)=>({high:0,medium:1,low:2}[a.priority]??9)-({high:0,medium:1,low:2}[b.priority]??9))}
function essaysNeedRewrite(d=Store.load()){return (d.essays||[]).filter(e=>(e.feedback||e.teacherComment)&&!(e.rewrites&&e.rewrites.length)&&e.status!=='final')}
function homeworkDue(d=Store.load()){return Object.entries(d.homework||{}).filter(([id,h])=>!['Reviewed','Mastered'].includes(h.status)).map(([id,h])=>({lessonId:id,...h}))}
function todayPlan(d=Store.load()){const nl=nextLesson(d), reviews=dueReviews(d), rewrites=essaysNeedRewrite(d), hw=homeworkDue(d);const arr=[{kind:'Learn',title:`${nl.chapterNo}.${nl.lessonNo} – ${nl.title}`,desc:nl.objective,time:nl.estimatedMinutes||30,href:`lesson.html?lesson=${nl.id}`},{kind:'Skill Practice',title:`${nl.skillName||nl.skill} drills`,desc:'Làm đúng 1 skill của lesson hiện tại, không viết full essay quá sớm.',time:12,href:`lesson.html?lesson=${nl.id}`}];if(reviews[0])arr.push({kind:'Debug',title:reviews.slice(0,3).map(x=>x.errorType).join(', '),desc:'Review lỗi đang lặp lại trong Error Log.',time:10,href:'review-queue.html'});if(hw[0])arr.push({kind:'Homework',title:Tutor.lessonById(hw[0].lessonId)?.title||'Homework',desc:'Nộp bài để có teacher feedback.',time:20,href:`homework.html?lesson=${hw[0].lessonId}`});if(rewrites[0])arr.push({kind:'Rewrite',title:rewrites[0].title||'Essay Draft cần rewrite',desc:'Rewrite sau feedback để deploy final essay.',time:25,href:'essays.html'});return arr}
function flowLine(status){const steps=['learn','practice','homework','reviewed','rewrite','mastered'];const i=steps.indexOf(status);return `<div class="flow-line">${steps.map((s,idx)=>`<span class="${idx<i?'done':idx===i?'current':status==='locked'?'locked':''}">${statusLabel(s)}</span>`).join('')}</div>`}
function bandChart(history,p){const vals=(history||[]).slice().reverse().slice(-10).map(x=>Number(x.overallBand||x.overall||0)).filter(Boolean);if(!vals.length)vals.push(Number(p.currentBand||5.5));return `<div class="chart-bars">${vals.map((v,i)=>`<div class="bar-row"><span>Entry ${i+1}</span><div class="bar-track"><div class="bar-fill" style="width:${UI.pct(v,9)}%"></div></div><b>${v.toFixed(1)}</b></div>`).join('')}</div>`}
function chapterProgress(d){return IELTS_DATA.chapters.map(c=>{const ls=IELTS_DATA.lessons.filter(l=>l.chapterNo===c.no);const done=ls.filter(l=>currentStatus(l.id,d)==='mastered').length;return `<p><b>Chapter ${c.no}: ${UI.esc(c.title)}</b> <span class="muted">${done}/${ls.length}</span></p><div class="progress"><span style="width:${UI.pct(done,ls.length)}%"></span></div>`}).join('')}
function badgeGrid(d){const names=['First Lesson','First Homework','First Essay','First Rewrite','7-Day Streak','Brainstorm Master','Introduction Master','Body Paragraph Builder','Full Essay Finisher'];return `<div class="badge-grid">${names.map(n=>`<span class="badge ${d.badges&&d.badges[n]?'earned':''}">${d.badges&&d.badges[n]?'🏆':'🔒'} ${n}</span>`).join('')}</div>`}
function renderReviewMini(d){const arr=dueReviews(d).slice(0,5);return arr.length?arr.map(q=>`<div class="review-item"><div><b>${UI.esc(q.errorType)}</b><p>${UI.esc(q.action||Tutor.reviewTaskFor(q.errorType))}</p></div><span class="pill ${q.priority==='high'?'purple':'blue'}">${q.priority||'medium'}</span></div>`).join(''):'<div class="empty">Không có lỗi cần review hôm nay. Giữ streak nha.</div>'}
function startSessionModal(){const plan=todayPlan();const start=Date.now();const m=UI.modal('Daily Study Session',`<p>Learning console đang chạy. Tick task đã làm rồi End Session.</p><div class="session-timer" id="timer">00:00</div>${plan.map((p,i)=>`<label class="check-item"><input type="checkbox" data-session-task="${i}"> ${UI.esc(p.kind+': '+p.title)}</label>`).join('')}<label class="label">Reflection</label><textarea id="sessionReflection" placeholder="Hôm nay em học được gì?"></textarea>`,`<button class="btn primary" id="endSession">End Session</button>`);const int=setInterval(()=>{const s=Math.floor((Date.now()-start)/1000),mm=String(Math.floor(s/60)).padStart(2,'0'),ss=String(s%60).padStart(2,'0');const el=m.querySelector('#timer');if(el)el.textContent=mm+':'+ss;else clearInterval(int)},1000);m.querySelector('#endSession').onclick=()=>{clearInterval(int);const done=[...m.querySelectorAll('[data-session-task]')].filter(c=>c.checked).length;const mins=Math.max(1,Math.round((Date.now()-start)/60000));Store.update(d=>{d.studySessions.unshift({id:Store.uid('session'),startedAt:new Date(start).toISOString(),endedAt:new Date().toISOString(),timeSpent:mins,completedTasks:done,reflection:m.querySelector('#sessionReflection').value,xpEarned:done*5});return d});Tutor.addXP(done*5,'Study session saved');m.remove();renderDashboard()}}

function renderDashboard(){const d=Store.load(),p=d.studentProfile,nl=nextLesson(d),days=dateDiff(p.expectedExamDate),plan=todayPlan(d),total=plan.reduce((s,x)=>s+(x.time||0),0),week=Store.weeklyStats(),goal=p.dailyGoal||{};title('Learning Console',`${p.name||'Học viên'} · Band ${p.currentBand} → ${p.targetBand} · Debug your mistakes`);document.getElementById('view').innerHTML=`<div class="dashboard-hero"><section class="card hero code-hero"><span class="code-kicker">// Today's Mission</span><h1>Compile your essay skills — ${total} minutes</h1><div class="today-list">${plan.map((it,i)=>`<div class="today-task"><div class="task-num">${i+1}</div><div><b>${it.kind}: ${UI.esc(it.title)}</b><p>${UI.esc(it.desc)}</p></div><a class="btn" href="${it.href}">Open</a></div>`).join('')}</div><div class="mini-actions"><a class="btn primary glow" href="lesson.html?lesson=${nl.id}">Run Today’s Lesson →</a><button class="btn" id="startSession">Start Study Session</button></div></section><section class="card console-card"><h3>Exam Countdown</h3><div class="countdown">${days===null?'—':Math.max(0,days)}</div><p>ngày tới ngày thi dự kiến</p><div class="progress animated"><span style="width:${UI.pct(Math.max(0,90-(days||90)),90)}%"></span></div><hr><h3>Band Progress</h3><div class="metric-value">${Tutor.prediction()}</div><p>Target: ${p.targetBand}</p></section></div><div class="stat-strip"><div class="card metric"><span class="muted">XP</span><span class="metric-value">${d.xp||0}</span></div><div class="card metric"><span class="muted">Streak</span><span class="metric-value">${d.streak.count||0}🔥</span></div><div class="card metric"><span class="muted">Pipeline</span><span class="metric-value">${Tutor.progress()}%</span></div><div class="card metric"><span class="muted">Review</span><span class="metric-value">${dueReviews(d).length}</span></div><div class="card metric"><span class="muted">Rewrite</span><span class="metric-value">${essaysNeedRewrite(d).length}</span></div><div class="card metric"><span class="muted">Exercises</span><span class="metric-value">${IELTS_DATA.exerciseBank.length}</span></div></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Weekly Goal</h3><p>${week.uniqueLessons}/${p.sessionsPerWeek||4} lessons · ${week.homework} homework · ${week.essays}/${goal.essaysPerWeek||1} essays · ${week.minutes} phút</p><div class="progress animated"><span style="width:${UI.pct(week.uniqueLessons,p.sessionsPerWeek||4)}%"></span></div><p class="muted">Goal mỗi ngày: ${goal.lessons||1} lesson · ${goal.minutes||45} phút · ${goal.exercises||5} micro exercises.</p></section><section class="card"><h3>Error Log hôm nay</h3>${renderReviewMini(d)}</section></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Continue Pipeline</h3>${flowLine(currentStatus(nl.id,d))}<p><b>${nl.chapterNo}.${nl.lessonNo} ${UI.esc(nl.title)}</b></p><p>Skill hiện tại: ${UI.esc(nl.skillName||nl.skill)}. Không Mastered nếu chưa Skill Practice → Homework → Feedback → Rewrite/Reflection.</p></section><section class="card"><h3>Band Tracker nhanh</h3>${bandChart(d.bandHistory,p)}</section></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Chapter progress</h3>${chapterProgress(d)}</section><section class="card"><h3>Badges</h3>${badgeGrid(d)}</section></div>`;document.getElementById('startSession').onclick=startSessionModal}

function lessonExerciseCount(l){return (l.guidedPractice||[]).length+(l.skillPractice||[]).length+(l.homeworkItems||[]).length+1}
function renderRoadmap(){const d=Store.load();title('Learning Roadmap','Mỗi chapter là một learning module có objective, skills, checkpoint, review và mastery. Không còn là danh sách lesson đơn thuần.');const html=IELTS_DATA.chapters.map(c=>{const ls=IELTS_DATA.lessons.filter(l=>l.chapterNo===c.no);const prog=UI.pct(ls.filter(l=>currentStatus(l.id,d)==='mastered').length,ls.length);return `<section class="card roadmap-chapter v3-roadmap"><div class="chapter-head"><div><span class="code-kicker">module.${String(c.no).padStart(2,'0')}</span><h2>Chapter ${c.no}: ${UI.esc(c.title)}</h2><p>${UI.esc(c.chapterObjective||c.description||'')}</p></div><div class="chapter-meter"><b>${prog}%</b><span>mastery</span><div class="progress animated"><span style="width:${prog}%"></span></div></div></div><div class="chapter-grid"><div><b>Estimated time</b><p>${UI.esc(c.estimatedTime||'—')}</p></div><div><b>Skills sẽ học</b><p>${(c.skills||[]).map(x=>`<span class="pill purple">${UI.esc(x)}</span>`).join(' ')}</p></div><div><b>Chapter checkpoint</b><p>${UI.esc(c.chapterCheckpoint||'Mini test + reflection + teacher review')}</p></div><div><b>Chapter review</b><p>${UI.esc(c.chapterReview||'Review weak skills before unlocking next chapter.')}</p></div><div><b>Chapter mastery</b><p>${UI.esc(c.chapterMastery||'Mastery requires feedback and rewrite.')}</p></div></div><div class="roadmap-tree">${ls.map((l,i)=>`<div class="tree-row"><div class="tree-line">${i===ls.length-1?'└──':'├──'}</div>${lessonCard(l,d)}</div>`).join('')}<div class="checkpoint-card">✅ ${UI.esc(c.chapterCheckpoint||'Checkpoint')}</div></div></section>`}).join('');document.getElementById('view').innerHTML=html;document.querySelectorAll('[data-unlock]').forEach(b=>b.onclick=()=>{Store.update(d=>{d.lessonStatus[b.dataset.unlock]='learn';return d});UI.toast('Unlocked manually');renderRoadmap()})}
function lessonCard(l,d){const st=currentStatus(l.id,d),locked=st==='locked',hw=d.homework[l.id]?.status||'Not Started',req=Tutor.prerequisites(l.id).slice(0,3).join(' · ')||'None';return `<div class="card lesson-card ${locked?'locked':''} ${st==='mastered'?'mastered-glow':''}"><div class="lesson-meta"><span class="pill status-${st}">${statusLabel(st)}</span><span class="pill blue">${l.estimatedMinutes} mins</span><span class="pill purple">${UI.esc(l.difficulty)}</span></div><h3>${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</h3><p>${UI.esc(l.objective)}</p><div class="req"><b>Skill:</b> ${UI.esc(l.skillName||l.skill)}<br><b>Prerequisites:</b> ${UI.esc(req)}<br><b>Micro exercises:</b> ${lessonExerciseCount(l)} · <b>Homework:</b> ${UI.esc(hw)}</div><div class="mini-actions">${locked?`<button class="btn ghost" data-unlock="${l.id}">Unlock manually</button>`:`<a class="btn primary" href="lesson.html?lesson=${l.id}">Open Lesson</a>`}</div></div>`}
function selectedLesson(){const id=new URLSearchParams(location.search).get('lesson');return Tutor.lessonById(id)||nextLesson()}
function renderLesson(){const l=selectedLesson(),d=Store.load(),st=currentStatus(l.id,d),work=d.lessonWork[l.id]||{};title('Lesson Classroom',`${l.chapterTitle} · one skill only: ${l.skillName||l.skill}`);if(st==='locked'&&!d.settings.developerMode&&!d.settings.teacherMode){document.getElementById('view').innerHTML=`<div class="card empty locked-blur"><h3>🔒 Lesson đang khóa</h3><p>Em cần hoàn thành prerequisite trước. Full Essay chỉ mở sau Introduction + Body + Conclusion.</p><button class="btn" id="unlockNow">Unlock manually</button></div>`;document.getElementById('unlockNow').onclick=()=>{Store.update(x=>{x.lessonStatus[l.id]='learn';return x});renderLesson()};return}document.getElementById('view').innerHTML=`<section class="card lesson-header"><div class="lesson-meta"><span class="pill">Chapter ${l.chapterNo}</span><span class="pill purple">${UI.esc(l.skillName||l.skill)}</span><span class="pill blue">${l.estimatedMinutes} mins</span><span class="pill">${UI.esc(l.difficulty)}</span><span class="pill status-${st}">Status: ${statusLabel(st)}</span></div><h1>${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</h1><p class="success-box"><b>Lesson Design Rule:</b> bài này chỉ luyện <b>${UI.esc(l.skillName||l.skill)}</b>. Không nhảy sang skill khác.</p>${flowLine(st)}<div class="mini-actions"><button class="btn ask-teacher" id="askLesson">💬 Ask Chị / Ask Teacher</button><button class="btn success" id="tryMaster">Mark Mastered</button></div></section><div class="tabs sticky">${tabKeys.map((t,i)=>`<button class="tab ${i===0?'active':''}" data-tab="${t[0]}">${t[1]}</button>`).join('')}</div><div class="tab-panels">${lessonPanels(l,work,d)}</div>`;document.querySelectorAll('[data-tab]').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.tab-panel').forEach(p=>p.classList.remove('active'));document.getElementById('tab-'+btn.dataset.tab).classList.add('active')});wireLesson(l);document.getElementById('askLesson').onclick=()=>Tutor.ask({chapter:l.chapterTitle,lesson:l.title,skill:l.skillName||l.skill,status:st,taskType:'Lesson',problem:'Em muốn hiểu bài này và biết có nên qua bài tiếp theo chưa.'});document.getElementById('tryMaster').onclick=()=>{if(Tutor.canMaster(l.id)){Tutor.setLessonStatus(l.id,'mastered',work.reflection||'',0);Tutor.addXP(25,'Lesson Mastered');renderLesson()}else UI.modal('Chưa đủ điều kiện Mastered',`<div class="danger-box">Cần: Skill Practice đã lưu, Homework đã submit, có Feedback/Teacher Note, và Rewrite hoặc Reflection.</div>`)} }
function exerciseDetails(ex){return `<div class="exercise-detail v4-exercise"><div><b>Objective</b><p>${UI.esc(ex.objective||'')}</p></div><div><b>Situation</b><p>${UI.esc(ex.situation||'')}</p></div>${ex.level?`<div><b>Level</b><p>${UI.esc(ex.level)}</p></div>`:''}${ex.context?`<div><b>Context</b><p class="context-prompt">${UI.esc(ex.context)}</p></div>`:''}<div><b>Task</b><p>${UI.esc(ex.task||ex.question||'')}</p></div><div><b>Requirement</b><p>${UI.esc(ex.requirement||ex.instruction||'')}</p></div>${ex.hint?`<div class="hint-box"><b>Hint</b><p>${UI.esc(ex.hint)}</p></div>`:''}${ex.miniHint?`<div class="hint-box mini"><b>Mini Hint</b><p>${UI.esc(ex.miniHint)}</p></div>`:''}<div><b>Example</b><p>${UI.esc(ex.example||'')}</p></div><div><b>Checklist</b><ul>${(ex.checklist||[]).map(x=>`<li>${UI.esc(x)}</li>`).join('')}</ul></div>${ex.immediateFeedback?`<div class="feedback-box"><b>Immediate Feedback</b><p>${UI.esc(ex.immediateFeedback)}</p></div>`:''}<div><b>Expected Outcome</b><p>${UI.esc(ex.expectedOutcome||'')}</p></div></div>`}
function resourceLinks(l){const r=l.resources||{};const link=(label,path,cls='')=>path?`<a class="btn ${cls}" target="_blank" href="../${UI.attr(path)}">${label}</a>`:'';return `<div class="resource-grid">${link('📄 Download Lesson PDF',r.lessonPdf,'primary glow')}${link('💬 Vocabulary Sheet',r.vocabularySheet)}${link('🧩 Grammar Notes',r.grammarNotes)}${link('🏆 Band 9 Example',r.band9Example)}${link('📚 Reference',r.reference)}</div>`}
function lessonPanels(l,work,d){const skill=l.skillName||l.skill;const tb=l.theoryBlocks||[];const ga=l.guidedAnalysis||{};return `<section class="card tab-panel active" id="tab-overview"><span class="code-kicker">lesson.pipeline.v4</span><h3>Overview</h3><div class="overview-grid"><div><b>Current Skill</b><p>${UI.esc(l.overview?.currentSkill||skill)}</p></div><div><b>Why this lesson</b><p>${UI.esc(l.overview?.whyThisLesson||l.objective||'')}</p></div><div><b>Where it fits</b><p>${UI.esc(l.overview?.whereItFits||'Roadmap → Practice → Feedback → Rewrite')}</p></div><div><b>Do not do</b><p>${UI.esc(l.overview?.doNotDo||'Không nhảy sang skill khác.')}</p></div></div><h3>Learning Objective</h3><p class="success-box">${UI.esc(l.learningObjective||l.lessonObjective||l.objective)}</p><h3>Lesson Resources</h3>${resourceLinks(l)}<button class="btn primary glow" id="saveLearn">Run Learn → Skill Practice</button></section><section class="card tab-panel" id="tab-theory"><h3>Theory: ${UI.esc(skill)}</h3><p class="warning-box"><b>Không đọc lướt:</b> Theory phải trả lời What, Why, When, How, Common mistakes và Checklist trước khi em làm bài.</p>${tb.length?tb.map(b=>`<div class="theory-block"><h4>${UI.esc(b.title)}</h4><p>${UI.esc(b.text)}</p></div>`).join(''):(l.theory||[]).map(p=>`<p>${UI.esc(p)}</p>`).join('')}<div class="success-box"><b>Design rule:</b> Theory, Guided Analysis, Skill Practice và Homework của bài này đều chỉ xoay quanh <b>${UI.esc(skill)}</b>.</div></section><section class="card tab-panel" id="tab-examples"><h3>Examples</h3>${(l.examples||[]).map(e=>`<div class="example-box"><b>${UI.esc(e.label)}</b><p>${UI.esc(e.text)}</p></div>`).join('')}</section><section class="card tab-panel" id="tab-analysis"><h3>${UI.esc(ga.sampleTitle||'Guided Analysis')}</h3><p class="context-prompt">${UI.esc(ga.sampleText||'')}</p><div class="analysis-grid">${(ga.steps||[]).map(s=>`<div class="analysis-step"><b>${UI.esc(s.label)}</b><p>${UI.esc(s.explanation)}</p></div>`).join('')}</div></section><section class="card tab-panel" id="tab-skill"><h3>Skill Practice: ${UI.esc(skill)}</h3><p class="warning-box">Skill Practice = có hướng dẫn. IELTS prompt nếu có chỉ là <b>Context</b>, không phải yêu cầu viết nguyên bài.</p><h3>Guided Practice: scaffolded, có hint</h3>${(l.guidedPractice||[]).map((g,i)=>practiceBox(l.id,`guided-${i}`,`${i+1}. ${g.title||g.task}`,g,d)).join('')}<h3>Independent Practice: context mới, ít gợi ý hơn</h3>${(l.skillPractice||[]).map((ex,i)=>practiceBox(l.id,`skill-${i}`,`${i+1}. ${ex.title||ex.task}`,ex,d)).join('')}</section><section class="card tab-panel" id="tab-homework"><h3>${UI.esc(l.homework?.title||'Independent Homework')}</h3><p class="danger-box"><b>Homework = tự làm.</b> Không hint, không scaffold, không copy Practice. AI/Teacher chỉ chấm sau khi nộp.</p><ul class="list-clean">${(l.homework?.instructions||[]).map(x=>`<li>${UI.esc(x)}</li>`).join('')}</ul>${(l.homeworkItems||[]).map((ex,i)=>`<div class="homework-micro"><label class="label">${i+1}. ${UI.esc(ex.title||ex.task)}</label>${exerciseDetails(ex)}<textarea data-hw-key="${l.id}-${i}">${UI.esc((d.homework[l.id]?.items||{})[i]||'')}</textarea></div>`).join('')}<label class="label">Bài làm / ghi chú tổng hợp</label><textarea id="lessonHomework">${UI.esc(d.homework[l.id]?.answer||'')}</textarea><div class="mini-actions"><button class="btn" id="saveLessonHomework">Save Homework Draft</button><button class="btn primary" id="submitLessonHomework">Submit Homework</button><button class="btn ghost ask-teacher" id="askHomework">💬 Ask Chị / Ask Teacher</button></div></section><section class="card tab-panel" id="tab-feedback"><h3>AI Feedback / Teacher Feedback</h3><p class="muted">AI phải biết Current Skill và chỉ chữa đúng lesson này.</p><div class="success-box"><b>AI focus:</b> ${UI.esc(l.aiFeedbackGuide?.focus||('Please focus on '+skill+' only.'))}</div><div class="criteria-grid">${(l.aiFeedbackGuide?.format||[]).map(x=>`<div class="criterion">${UI.esc(x)}</div>`).join('')}</div><textarea id="lessonFeedback" placeholder="Dán AI/teacher feedback ở đây">${UI.esc(work.feedback||'')}</textarea><div class="form-row"><input class="input" id="fbStrength" placeholder="Strengths" value="${UI.attr(work.strengths||'')}"><input class="input" id="fbWeak" placeholder="Weaknesses" value="${UI.attr(work.weaknesses||'')}"></div><button class="btn primary" id="saveLessonFeedback">Save Feedback → Rewrite</button></section><section class="card tab-panel" id="tab-rewrite"><h3>Rewrite after feedback</h3><p class="warning-box">Rewrite đúng phần được chữa. Không viết lại toàn bài nếu lesson không yêu cầu.</p>${l.rewriteTask?exerciseDetails(l.rewriteTask):''}<textarea id="lessonRewrite" placeholder="Viết lại phần đã được chữa">${UI.esc(work.rewrite||'')}</textarea><button class="btn primary" id="saveLessonRewrite">Save Rewrite</button></section><section class="card tab-panel" id="tab-reflection"><h3>Reflection</h3>${(l.reflectionPrompts||['Hôm nay em học được gì?','Em còn thấy khó chỗ nào?']).map((q,i)=>`<label class="label">${UI.esc(q)}</label><textarea data-reflect="${i}">${UI.esc((work.reflections||[])[i]||'')}</textarea>`).join('')}<h3>Course QA Mode</h3>${qaMiniForm(l,d)}<button class="btn primary" id="saveReflection">Save Reflection</button></section><section class="card tab-panel" id="tab-mastered"><h3>Success Criteria → Mastered</h3><p class="danger-box"><b>Không Mastered chỉ vì đã đọc theory.</b> Mastered cần practice, homework, feedback, rewrite và reflection.</p><div class="criteria-grid">${(l.successCriteria||[]).map(x=>`<div class="criterion">${UI.esc(x)}</div>`).join('')}</div><h3>Review Quiz</h3>${(l.reviewQuiz||[]).map((q,i)=>`<div class="homework-micro"><label class="label">${i+1}. ${UI.esc(q.title||q.task)}</label>${exerciseDetails(q)}<textarea data-hw-key="${l.id}-quiz-${i}">${UI.esc((d.homework[l.id]?.items||{})['quiz-'+i]||'')}</textarea></div>`).join('')}<button class="btn success glow" id="masterFromPanel">Check Mastered Gate</button></section>`}
function qaMiniForm(l,d){const qa=(d.courseQAFeedback||[]).filter(x=>x.lessonId===l.id)[0]||{};const qs=IELTS_DATA.courseQA||[];return `<div class="qa-grid">${qs.map(q=>q.type==='textarea'?`<label class="label">${UI.esc(q.label)}</label><textarea id="${q.id}">${UI.esc(qa[q.id]||'')}</textarea>`:`<label class="label">${UI.esc(q.label)}</label><select id="${q.id}">${[1,2,3,4,5].map(n=>`<option value="${n}" ${Number(qa[q.id]||5)===n?'selected':''}>${'⭐'.repeat(n)} ${n}/5</option>`).join('')}</select>`).join('')}</div><button class="btn primary glow" id="saveCourseQA">Save Course QA</button>`}
function practiceBox(lid,key,title,ex,d){const k=`${lid}-${key}`;return `<div class="example-box exercise-box"><b>${UI.esc(title)}</b>${exerciseDetails(ex)}<textarea data-practice-key="${k}">${UI.esc(d.practice[k]||'')}</textarea><div class="mini-actions"><button class="btn savePractice" data-key="${k}">Save Skill Practice</button><button class="btn ghost askPractice" data-key="${k}" data-task="${UI.attr(title)}">Ask Teacher</button></div></div>`}
function wireLesson(l){document.getElementById('saveLearn').onclick=()=>{Tutor.setLessonStatus(l.id,'practice','Completed Learn tab',l.estimatedMinutes||10);Tutor.addXP(10,'Learn saved');renderLesson()};document.querySelectorAll('.savePractice').forEach(b=>b.onclick=()=>{const val=document.querySelector(`[data-practice-key="${b.dataset.key}"]`).value;Store.set('practice.'+b.dataset.key,val);Store.update(d=>{d.exerciseAnswers[b.dataset.key]=val;return d});Tutor.setLessonStatus(l.id,'homework','Skill Practice saved',8);Tutor.addXP(8,'Skill Practice saved')});document.querySelectorAll('.askPractice').forEach(b=>b.onclick=()=>Tutor.ask({chapter:l.chapterTitle,lesson:l.title,skill:l.skillName||l.skill,status:Tutor.statusOf(l.id),taskType:b.dataset.task,answer:document.querySelector(`[data-practice-key="${b.dataset.key}"]`).value,problem:'Em muốn biết câu trả lời Skill Practice này đã đúng skill chưa.'}));function saveHw(status){const items={};document.querySelectorAll('[data-hw-key]').forEach((t,i)=>items[i]=t.value);Store.update(d=>{d.homework[l.id]={lessonId:l.id,status,answer:document.getElementById('lessonHomework').value,items,updatedAt:new Date().toISOString(),submittedAt:status==='Submitted'?new Date().toISOString():d.homework[l.id]?.submittedAt,deadline:Store.addDays(l.homework?.deadlineOffsetDays||4)};return d});if(status==='Submitted')Tutor.setLessonStatus(l.id,'reviewed','Homework submitted',15);UI.toast(status==='Submitted'?'Homework submitted':'Homework draft saved')}
document.getElementById('saveLessonHomework').onclick=()=>saveHw('Doing');document.getElementById('submitLessonHomework').onclick=()=>saveHw('Submitted');document.getElementById('askHomework').onclick=()=>Tutor.ask({chapter:l.chapterTitle,lesson:l.title,skill:l.skillName||l.skill,status:Tutor.statusOf(l.id),taskType:'Homework',answer:document.getElementById('lessonHomework').value,problem:'Em muốn được chữa homework đúng kỹ năng của lesson này và được giao rewrite nếu cần.'});document.getElementById('saveLessonFeedback').onclick=()=>{Store.update(d=>{d.lessonWork[l.id]={...(d.lessonWork[l.id]||{}),feedback:lessonFeedback.value,strengths:fbStrength.value,weaknesses:fbWeak.value};d.feedback.unshift({id:Store.uid('fb'),date:Store.today(),lessonId:l.id,studentWork:d.homework[l.id]?.answer||'',strengths:fbStrength.value,weaknesses:fbWeak.value,teacherComment:lessonFeedback.value});return d});Tutor.setLessonStatus(l.id,'rewrite','Feedback saved',0);Tutor.addXP(10,'Feedback saved')};document.getElementById('saveLessonRewrite').onclick=()=>{Store.update(d=>{d.lessonWork[l.id]={...(d.lessonWork[l.id]||{}),rewrite:lessonRewrite.value};if(d.homework[l.id])d.homework[l.id].status='Rewrite';return d});Tutor.setLessonStatus(l.id,'rewrite','Rewrite saved',20);Tutor.addXP(15,'Rewrite saved')};document.getElementById('saveReflection').onclick=()=>{Store.update(d=>{d.lessonWork[l.id]={...(d.lessonWork[l.id]||{}),reflections:[...document.querySelectorAll('[data-reflect]')].map(x=>x.value),reflection:[...document.querySelectorAll('[data-reflect]')].map(x=>x.value).join('\n')};return d});UI.toast('Reflection saved')};const masterPanel=document.getElementById('masterFromPanel');if(masterPanel)masterPanel.onclick=()=>{if(Tutor.canMaster(l.id)){Tutor.setLessonStatus(l.id,'mastered',work.reflection||'',0);Tutor.addXP(25,'Lesson Mastered');renderLesson()}else UI.modal('Chưa đủ điều kiện Mastered',`<div class="danger-box">Cần đủ: Skill Practice, Homework, AI/Teacher Feedback, Rewrite, Reflection và Review Quiz.</div>`)};const saveQA=document.getElementById('saveCourseQA');if(saveQA)saveQA.onclick=()=>{const item={id:Store.uid('qa'),lessonId:l.id,lessonTitle:l.title,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};(IELTS_DATA.courseQA||[]).forEach(q=>{const el=document.getElementById(q.id);item[q.id]=el?el.value:''});Store.update(d=>{d.courseQAFeedback=(d.courseQAFeedback||[]).filter(x=>x.lessonId!==l.id);d.courseQAFeedback.unshift(item);return d});UI.toast('Course QA saved. Cảm ơn góp ý thật!')}}

function renderExerciseBank(){const d=Store.load();title('Exercise Bank','Micro exercises có Objective → Situation → Task → Requirement → Example → Checklist → Expected Outcome. Generate thêm bằng template offline.');const skills=[...new Set(IELTS_DATA.exerciseBank.map(e=>e.skillName||e.skill))].sort();const topics=[...new Set(IELTS_DATA.exerciseBank.map(e=>e.topic).filter(Boolean))].sort();const diffs=['Foundation','Core','Band Booster'];document.getElementById('view').innerHTML=`<section class="card"><div class="filter-row"><select id="exSkill"><option value="">All skills</option>${UI.selectOptions(skills)}</select><select id="exTopic"><option value="">All topics</option>${UI.selectOptions(topics)}</select><select id="exDiff"><option value="">All difficulty</option>${UI.selectOptions(diffs)}</select><select id="exStatus"><option value="">All status</option>${UI.selectOptions(['new','doing','done'])}</select></div><div class="mini-actions"><button class="btn primary" id="generateEx">Generate More Exercises</button><button class="btn" id="addCustomEx">Add Custom Exercise</button></div></section><div id="exerciseList" class="grid cols-3" style="margin-top:18px"></div>`;function allExercises(){return [...IELTS_DATA.exerciseBank,...(Store.load().customExercises||[])]}function draw(){const d=Store.load(),s=exSkill.value,t=exTopic.value,df=exDiff.value,st=exStatus.value;const arr=allExercises().filter(e=>(!s||(e.skillName||e.skill)===s)&&(!t||e.topic===t)&&(!df||e.difficulty===df)&&(!st||((d.exerciseStatus||{})[e.id]||e.status||'new')===st)).slice(0,90);exerciseList.innerHTML=arr.map(e=>`<div class="card exercise-card"><div class="lesson-meta"><span class="pill purple">${UI.esc(e.skillName||e.skill)}</span><span class="pill blue">${UI.esc(e.topic||'General')}</span><span class="pill">${UI.esc(e.difficulty||'Core')}</span></div><h3>${UI.esc(e.title||e.task)}</h3>${exerciseDetails(e)}<textarea data-exanswer="${e.id}" placeholder="Nhập câu trả lời micro exercise...">${UI.esc((d.exerciseAnswers||{})[e.id]||'')}</textarea><div class="mini-actions"><button class="btn saveExercise" data-id="${e.id}">Save</button><button class="btn success doneExercise" data-id="${e.id}">Done</button><button class="btn ghost askExercise" data-id="${e.id}">Ask Teacher</button></div></div>`).join('')||'<div class="empty">Không có bài phù hợp. Bấm Generate More Exercises.</div>';wireExerciseButtons()}[exSkill,exTopic,exDiff,exStatus].forEach(el=>el.onchange=draw);document.getElementById('generateEx').onclick=()=>{const skillName=exSkill.value||'Main Ideas',topic=exTopic.value||'Education';Store.update(d=>{for(let i=0;i<12;i++){const id=Store.uid('genex');d.customExercises.unshift({id,lessonId:'custom',skill:skillName.toLowerCase().replaceAll(' ','-'),skillName,topic,difficulty:['Foundation','Core','Band Booster'][i%3],type:'generated',status:'new',title:`Generated ${skillName} drill ${i+1}`,objective:`Luyện đúng kỹ năng ${skillName}.`,situation:`Teacher/custom mode tạo thêm bài khi học viên cần luyện ${skillName}.`,context:`Topic: ${topic}.`,task:`Complete one controlled answer for ${skillName} only.`,requirement:`Do not write a full essay. Follow the skill requirement only.`,example:`A good answer is short, clear and limited to ${skillName}.`,checklist:[`Only ${skillName}.`,'No full essay.','Save before asking teacher.'],expectedOutcome:`Học viên có thêm một câu trả lời luyện ${skillName}.`})}return d});UI.toast('Generated 12 structured exercises');draw()};document.getElementById('addCustomEx').onclick=()=>{const m=UI.modal('Add Custom Exercise',`<div class="form-row"><input class="input" id="cxTitle" placeholder="Title"><select id="cxSkill">${UI.selectOptions(skills)}</select></div><div class="form-row"><input class="input" id="cxTopic" placeholder="Topic"><select id="cxDiff">${UI.selectOptions(diffs,'Core')}</select></div><textarea id="cxQuestion" placeholder="Task / requirement"></textarea>`,`<button class="btn primary" id="saveCx">Save custom exercise</button>`);m.querySelector('#saveCx').onclick=()=>{Store.update(d=>{d.customExercises.unshift({id:Store.uid('cx'),title:cxTitle.value||'Custom exercise',skillName:cxSkill.value,skill:cxSkill.value.toLowerCase().replaceAll(' ','-'),topic:cxTopic.value||'General',difficulty:cxDiff.value,task:cxQuestion.value,objective:`Custom practice for ${cxSkill.value}.`,situation:'Giáo viên/học viên tự thêm bài luyện.',requirement:'Follow the custom instruction only.',example:'Keep the answer short and checkable.',checklist:['One skill only.','Clear answer.'],expectedOutcome:'Custom exercise completed.',status:'new',type:'custom'});return d});m.remove();UI.toast('Custom exercise added');draw()}};draw()}
function wireExerciseButtons(){document.querySelectorAll('.saveExercise').forEach(b=>b.onclick=()=>{const val=document.querySelector(`[data-exanswer="${b.dataset.id}"]`).value;Store.update(d=>{d.exerciseAnswers[b.dataset.id]=val;d.exerciseStatus[b.dataset.id]='doing';return d});Tutor.addXP(4,'Exercise saved')});document.querySelectorAll('.doneExercise').forEach(b=>b.onclick=()=>{Store.update(d=>{d.exerciseStatus[b.dataset.id]='done';return d});Tutor.addXP(6,'Exercise done')});document.querySelectorAll('.askExercise').forEach(b=>b.onclick=()=>{const ex=[...IELTS_DATA.exerciseBank,...Store.load().customExercises].find(x=>x.id===b.dataset.id);Tutor.ask({chapter:'Exercise Bank',lesson:Tutor.lessonById(ex?.lessonId)?.title||'',skill:ex?.skill||'',taskType:ex?.title||'Exercise',prompt:[ex?.context,ex?.task,ex?.requirement].filter(Boolean).join('\n'),answer:document.querySelector(`[data-exanswer="${b.dataset.id}"]`).value,problem:'Em muốn biết câu trả lời micro exercise này đã đúng skill chưa.'})})}
function renderPractice(){const l=nextLesson(), skill=l.skill;title('Skill Practice','Bài tập nhỏ theo đúng skill hiện tại, không viết full essay quá sớm.');const arr=IELTS_DATA.exerciseBank.filter(e=>e.lessonId===l.id||e.skill===skill).slice(0,24);document.getElementById('view').innerHTML=`<section class="card hero"><span class="code-kicker">run skill-practice</span><h1>${UI.esc(l.skillName||skill)}</h1><p>Active lesson: ${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</p><a class="btn" href="lesson.html?lesson=${l.id}">Open Lesson Classroom</a></section><div class="grid cols-3" style="margin-top:18px">${arr.map(e=>`<section class="card exercise-card"><span class="pill purple">${UI.esc(e.skillName||e.skill)}</span><h3>${UI.esc(e.title||e.task)}</h3>${exerciseDetails(e)}<textarea data-exanswer="${e.id}">${UI.esc((Store.load().exerciseAnswers||{})[e.id]||'')}</textarea><div class="mini-actions"><button class="btn saveExercise" data-id="${e.id}">Save</button><button class="btn ghost askExercise" data-id="${e.id}">Ask Teacher</button></div></section>`).join('')}</div>`;wireExerciseButtons()}
function renderHomework(){const d=Store.load();title('Homework','Homework theo lesson, trạng thái Not Started / Doing / Submitted / Reviewed / Rewrite / Mastered.');const lessons=IELTS_DATA.lessons.slice(0,70);document.getElementById('view').innerHTML=`<div class="grid cols-3">${lessons.map(l=>{const h=d.homework[l.id]||{};return `<section class="card homework-card"><span class="pill status-${currentStatus(l.id,d)}">${statusLabel(currentStatus(l.id,d))}</span><h3>${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</h3><p><b>Skill:</b> ${UI.esc(l.skillName||l.skill)}</p><p>Status: <b>${UI.esc(h.status||'Not Started')}</b></p><p>${(l.homeworkItems||[]).length} micro homework items</p><a class="btn primary" href="lesson.html?lesson=${l.id}">Open homework</a></section>`}).join('')}</div><section class="card" style="margin-top:18px"><h3>Custom Assignments</h3>${(d.customAssignments||[]).map(a=>`<div class="review-item"><div><b>${UI.esc(a.title)}</b><p>${UI.esc(a.skill)} · Due ${a.dueDate||'—'}</p></div><span class="pill">${UI.esc(a.status||'assigned')}</span></div>`).join('')||'<div class="empty">Chưa có custom assignment.</div>'}</section>`}
function renderEssays(){const d=Store.load(),prompts=IELTS_DATA.prompts.slice(0,280),current=Tutor.promptById(d.selectedPromptId)||prompts[0];title('Essay Workspace','Draft 1 → Teacher Feedback → Rewrite 1 → Rewrite 2 → Final Version → Band Estimate.');document.getElementById('view').innerHTML=`<div class="essay-grid"><section class="card"><h3>Build your argument</h3><select id="essayPrompt">${prompts.map(p=>`<option value="${p.id}" ${p.id===current.id?'selected':''}>${UI.esc(p.topic+' · '+p.essayType+' · '+p.question.slice(0,80))}</option>`).join('')}</select><p id="essayQuestion" class="warning-box">${UI.esc(current.question)}</p><textarea id="intro" placeholder="Introduction">${UI.esc(Store.get('essayDraft.intro',''))}</textarea><textarea id="body1" placeholder="Body 1">${UI.esc(Store.get('essayDraft.body1',''))}</textarea><textarea id="body2" placeholder="Body 2">${UI.esc(Store.get('essayDraft.body2',''))}</textarea><textarea id="conclusion" placeholder="Conclusion">${UI.esc(Store.get('essayDraft.conclusion',''))}</textarea><div class="mini-actions"><button class="btn" id="saveDraft">Save Draft</button><button class="btn" id="addEssayFeedback">Add Feedback</button><button class="btn" id="addRewrite">Add Rewrite</button><button class="btn success" id="markFinal">Deploy Final Essay</button><button class="btn ghost ask-teacher" id="askEssay">💬 Ask Teacher</button></div></section><aside class="card"><h3>Word Count</h3><div id="wcBox"></div><hr><h3>Band Estimate</h3><div class="form-row"><input class="input" id="tr" placeholder="TR"><input class="input" id="cc" placeholder="CC"></div><div class="form-row"><input class="input" id="lr" placeholder="LR"><input class="input" id="gr" placeholder="GRA"></div><textarea id="teacherComment" placeholder="Teacher comment"></textarea><button class="btn primary" id="saveBand">Save Band History</button></aside></div><section class="card" style="margin-top:18px"><h3>Revision History</h3><div id="revisionList">${(d.essays||[]).slice(0,8).map(e=>`<div class="revision-card"><span class="pill">${e.status||'draft'}</span><h3>${UI.esc(e.title||'Essay')}</h3><p>${UI.esc((e.prompt||'').slice(0,180))}</p><p>Rewrites: ${(e.rewrites||[]).length}</p></div>`).join('')||'<div class="empty">Chưa có essay.</div>'}</div></section>`;function updateWc(){const texts=[intro.value,body1.value,body2.value,conclusion.value],full=texts.join(' '),w=UI.wordCount(full);let warns=[];if(w<250)warns.push('dưới 250 từ');if(w>320)warns.push('quá dài trên 320 từ');if(!conclusion.value.trim())warns.push('chưa có conclusion');if(UI.wordCount(body1.value)<70||UI.wordCount(body2.value)<70)warns.push('body quá ngắn');wcBox.innerHTML=`<div class="metric-value">${w}</div><p>Intro ${UI.wordCount(intro.value)} · Body1 ${UI.wordCount(body1.value)} · Body2 ${UI.wordCount(body2.value)} · Conclusion ${UI.wordCount(conclusion.value)}</p><p class="word-warn ${warns.length?'danger-box':'success-box'}">${warns.join(' · ')||'Word count ổn.'}</p>`}['input','change'].forEach(ev=>document.querySelectorAll('#intro,#body1,#body2,#conclusion').forEach(x=>x.addEventListener(ev,updateWc)));essayPrompt.onchange=()=>{Store.update(d=>{d.selectedPromptId=essayPrompt.value;return d});renderEssays()};updateWc();saveDraft.onclick=()=>{const essay={id:Store.uid('essay'),title:'Draft '+(d.essays.length+1),prompt:current.question,essayType:current.essayType,topic:current.topic,draft:{intro:intro.value,body1:body1.value,body2:body2.value,conclusion:conclusion.value},rewrites:[],createdAt:new Date().toISOString(),status:'draft'};Store.update(x=>{x.essays.unshift(essay);x.currentEssayId=essay.id;x.revisionHistory[essay.id]=[{type:'draft 1',date:new Date().toISOString(),text:[intro.value,body1.value,body2.value,conclusion.value].join('\n\n')}];return x});Tutor.addXP(15,'Draft saved')};addEssayFeedback.onclick=()=>{const id=Store.load().currentEssayId;Store.update(x=>{const e=x.essays.find(e=>e.id===id)||x.essays[0];if(e){e.feedback=teacherComment.value||'Feedback added';e.status='feedback';x.revisionHistory[e.id]=(x.revisionHistory[e.id]||[]).concat([{type:'teacher feedback',date:new Date().toISOString(),text:e.feedback}])}return x});UI.toast('Feedback added')};addRewrite.onclick=()=>{const text=[intro.value,body1.value,body2.value,conclusion.value].join('\n\n');Store.update(x=>{const e=x.essays.find(e=>e.id===x.currentEssayId)||x.essays[0];if(e){e.rewrites=e.rewrites||[];e.rewrites.push({date:new Date().toISOString(),text});e.status='rewrite';x.revisionHistory[e.id]=(x.revisionHistory[e.id]||[]).concat([{type:'rewrite '+e.rewrites.length,date:new Date().toISOString(),text}])}return x});Tutor.addXP(20,'Rewrite saved')};markFinal.onclick=()=>{Store.update(x=>{const e=x.essays.find(e=>e.id===x.currentEssayId)||x.essays[0];if(e){e.finalVersion=[intro.value,body1.value,body2.value,conclusion.value].join('\n\n');e.status='final'}return x});Tutor.addXP(25,'Final essay deployed')};saveBand.onclick=()=>{const vals=[Number(tr.value),Number(cc.value),Number(lr.value),Number(gr.value)].filter(Boolean);const overall=vals.length?vals.reduce((a,b)=>a+b,0)/vals.length:Number(Store.load().studentProfile.currentBand||5.5);Store.update(x=>{x.bandHistory.unshift({date:Store.today(),essayId:x.currentEssayId,taskResponse:tr.value,coherence:cc.value,lexicalResource:lr.value,grammar:gr.value,overallBand:overall.toFixed(1),teacherComment:teacherComment.value});return x});UI.toast('Band history saved')};askEssay.onclick=()=>Tutor.ask({chapter:'Essay Workspace',lesson:'Full Essay Pipeline',skill:'full essay',taskType:'Essay draft',prompt:current.question,answer:[intro.value,body1.value,body2.value,conclusion.value].join('\n\n'),problem:'Em muốn được chữa theo 4 tiêu chí IELTS và giao rewrite.'})}
function renderPromptBank(){
  const d=Store.load(),q=new URLSearchParams(location.search).get('q')||'';
  title('Prompt Bank · IELTS Overload',`${IELTS_DATA.prompts.length} IELTS Task 2 prompts · 20 topics · 7 essay types. Prompt là context; exercise vẫn phải đúng skill lesson.`);
  const topics=IELTS_DATA.topics,types=IELTS_DATA.essayTypes,diffs=[...new Set(IELTS_DATA.prompts.map(p=>p.difficulty))];
  document.getElementById('view').innerHTML=`<section class="card code-hero"><span class="code-kicker">// IELTS prompt mountain</span><h3>Ngập đầu đề IELTS, nhưng vẫn học theo skill nhỏ</h3><p>Chọn prompt để luyện Main Idea, Topic Sentence, Paraphrase, Conclusion hoặc Full Essay khi đã unlock. Không bắt viết full essay quá sớm.</p><div class="stat-strip"><div class="metric"><span class="muted">Prompts</span><span class="metric-value">${IELTS_DATA.prompts.length}</span></div><div class="metric"><span class="muted">Topics</span><span class="metric-value">${topics.length}</span></div><div class="metric"><span class="muted">Types</span><span class="metric-value">${types.length}</span></div></div><div class="filter-row"><select id="typeFilter"><option value="">All essay types</option>${UI.selectOptions(types)}</select><select id="topicFilter"><option value="">All topics</option>${UI.selectOptions(topics)}</select><select id="diffFilter"><option value="">All difficulty</option>${UI.selectOptions(diffs)}</select><select id="statusFilter"><option value="">All status</option>${UI.selectOptions(['unused','practiced','essay written','mastered'])}</select></div><button class="btn primary glow" id="randomPrompt">Random Prompt</button><span id="promptCount" class="pill blue"></span></section><div id="promptList" class="grid cols-2" style="margin-top:18px"></div>`;
  function draw(){
    const all=IELTS_DATA.prompts.filter(p=>(!typeFilter.value||p.essayType===typeFilter.value)&&(!topicFilter.value||p.topic===topicFilter.value)&&(!diffFilter.value||p.difficulty===diffFilter.value)&&(!statusFilter.value||(d.promptStatus[p.id]||p.status||'unused')===statusFilter.value)&&(!q||p.question.toLowerCase().includes(q.toLowerCase())));
    const arr=all.slice(0,360);
    promptCount.textContent=`Showing ${arr.length}/${all.length}`;
    promptList.innerHTML=arr.map(p=>`<div class="card prompt-card"><div class="lesson-meta"><span class="pill purple">${UI.esc(p.essayType)}</span><span class="pill blue">${UI.esc(p.topic)}</span><span class="pill">${UI.esc(p.difficulty)}</span><span class="pill green">${UI.esc(d.promptStatus[p.id]||p.status||'unused')}</span></div><h3>${UI.esc(p.question)}</h3><p class="muted"><b>Use as context:</b> ${UI.esc(p.promptRole||'Use this as IELTS context for a micro skill.')}</p><p><b>Main ideas:</b> ${(p.suggestedMainIdeas||[]).slice(0,2).map(UI.esc).join(' · ')}</p><p><b>Vocabulary:</b> ${(p.usefulVocabulary||p.vocabularySuggestions||[]).slice(0,5).map(UI.esc).join(', ')}</p><p><b>Structure:</b> ${UI.esc(Array.isArray(p.recommendedStructure)?p.recommendedStructure.join(' → '):(p.recommendedStructure||p.possibleStructure||''))}</p><div class="prompt-tools"><button class="btn usePractice" data-id="${p.id}">Use for Skill Practice</button><button class="btn primary useEssay" data-id="${p.id}">Use for Essay</button><button class="btn useHomework" data-id="${p.id}">Use for Homework</button></div></div>`).join('')||'<div class="empty">Không tìm thấy prompt phù hợp.</div>';
    wirePromptButtons();
  }
  [typeFilter,topicFilter,diffFilter,statusFilter].forEach(el=>el.onchange=draw);
  randomPrompt.onclick=()=>{const p=IELTS_DATA.prompts[Math.floor(Math.random()*IELTS_DATA.prompts.length)];typeFilter.value=p.essayType;topicFilter.value=p.topic;diffFilter.value=p.difficulty;draw();UI.toast('Random prompt selected')};
  draw();
}
function wirePromptButtons(){document.querySelectorAll('.usePractice').forEach(b=>b.onclick=()=>{Store.update(d=>{d.selectedPromptId=b.dataset.id;d.promptStatus[b.dataset.id]='practiced';return d});location.href='practice.html'});document.querySelectorAll('.useEssay').forEach(b=>b.onclick=()=>{Store.update(d=>{d.selectedPromptId=b.dataset.id;d.promptStatus[b.dataset.id]='essay written';return d});location.href='essays.html'});document.querySelectorAll('.useHomework').forEach(b=>b.onclick=()=>{const p=Tutor.promptById(b.dataset.id);Store.update(d=>{d.customAssignments.unshift({id:Store.uid('assign'),title:'Prompt Bank Homework',skill:'outline/full essay',prompt:p.question,instruction:'Outline first. Only write full essay if Full Essay is unlocked.',difficulty:p.difficulty,dueDate:Store.addDays(3),relatedLesson:'ch06-l01',status:'assigned'});d.promptStatus[b.dataset.id]='practiced';return d});UI.toast('Custom homework created')})}
function renderErrorLog(){const d=Store.load();title('Error Log','Sổ lỗi học tập của em — mỗi lỗi giống một bug report.');const types=Object.keys(Tutor.errorLessonMap);document.getElementById('view').innerHTML=`<section class="card"><h3>New Bug Report</h3><div class="form-row"><select id="errType">${UI.selectOptions(types)}</select><select id="errPriority">${UI.selectOptions(['high','medium','low'],'medium')}</select></div><div class="form-row"><textarea id="errOriginal" placeholder="Original sentence"></textarea><textarea id="errCorrected" placeholder="Fixed sentence"></textarea></div><textarea id="errExplain" placeholder="Vietnamese explanation"></textarea><button class="btn primary" id="addError">Add to Error Log</button></section><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Lỗi theo loại</h3>${errorChart(d.errors)}</section><section class="card"><h3>Lỗi lặp nhiều nhất</h3>${(d.errors||[]).slice().sort((a,b)=>(b.frequency||1)-(a.frequency||1)).slice(0,8).map(e=>`<div class="review-item"><div><b>${UI.esc(e.errorType)}</b><p>${UI.esc(e.vietnameseExplanation||e.originalSentence||'')}</p></div><span class="pill">x${e.frequency||1}</span></div>`).join('')||'<div class="empty">Chưa có lỗi.</div>'}</section></div><section class="card" style="margin-top:18px"><h3>Bug list</h3><table class="table"><thead><tr><th>Bug type</th><th>Original</th><th>Fixed</th><th>Priority</th><th>Status</th><th>Action</th></tr></thead><tbody>${(d.errors||[]).map(e=>`<tr><td>${UI.esc(e.errorType)}</td><td>${UI.esc(e.originalSentence)}</td><td>${UI.esc(e.correctedSentence)}</td><td>${UI.esc(e.priority||'medium')}</td><td>${UI.esc(e.status||'active')}</td><td><button class="btn createReview" data-id="${e.id}">Create Review Task</button><button class="btn ghost askError" data-id="${e.id}">Ask Teacher</button></td></tr>`).join('')}</tbody></table></section>`;addError.onclick=()=>{Store.update(d=>{const existing=d.errors.find(e=>e.errorType===errType.value&&e.originalSentence===errOriginal.value);if(existing){existing.frequency=(Number(existing.frequency)||1)+1;existing.lastSeen=Store.today();existing.status='active'}else d.errors.unshift({id:Store.uid('err'),errorType:errType.value,originalSentence:errOriginal.value,correctedSentence:errCorrected.value,vietnameseExplanation:errExplain.value,relatedLesson:Tutor.errorLessonMap[errType.value],frequency:1,firstSeen:Store.today(),lastSeen:Store.today(),status:'active',priority:errPriority.value});return d});Tutor.runAdaptiveRules();UI.toast('Bug logged');renderErrorLog()};document.querySelectorAll('.createReview').forEach(b=>b.onclick=()=>{const e=Store.load().errors.find(x=>x.id===b.dataset.id);Tutor.addReview({errorType:e.errorType,relatedLessonId:e.relatedLesson,priority:e.priority||'medium',action:Tutor.reviewTaskFor(e.errorType)})});document.querySelectorAll('.askError').forEach(b=>b.onclick=()=>{const e=Store.load().errors.find(x=>x.id===b.dataset.id);Tutor.ask({chapter:'Error Log',lesson:Tutor.lessonById(e.relatedLesson)?.title||'',skill:e.errorType,status:e.status,taskType:'Bug report',answer:`Original: ${e.originalSentence}\nFixed: ${e.correctedSentence}`,problem:e.vietnameseExplanation})})}
function errorChart(errors){const map={};(errors||[]).forEach(e=>map[e.errorType]=(map[e.errorType]||0)+(Number(e.frequency)||1));const max=Math.max(1,...Object.values(map));return `<div class="chart-bars">${Object.entries(map).map(([k,v])=>`<div class="bar-row"><span>${UI.esc(k)}</span><div class="bar-track"><div class="bar-fill" style="width:${UI.pct(v,max)}%"></div></div><b>${v}</b></div>`).join('')||'<div class="empty">Chưa có dữ liệu lỗi.</div>'}</div>`}
function renderReviewQueue(){const d=Store.load();title('Review Queue','Lỗi lặp lại, lesson chưa mastered, essay cần rewrite, vocabulary chưa mastered.');const lessonItems=IELTS_DATA.lessons.filter(l=>['practice','homework','reviewed','rewrite'].includes(currentStatus(l.id,d))).slice(0,12).map(l=>({id:'lesson-'+l.id,errorType:'Lesson not mastered',relatedLessonId:l.id,priority:'medium',nextReviewDate:Store.today(),action:'Continue pipeline to Mastered',status:'pending'}));const vocabItems=IELTS_DATA.vocabulary.filter(v=>(d.vocabulary[v.id]?.status||v.status||'New')!=='Mastered').slice(0,8).map(v=>({id:'vocab-'+v.id,errorType:'Vocabulary review',priority:'low',nextReviewDate:Store.today(),action:`Review: ${v.phrase||v.word}`,status:'pending'}));const all=[...(d.reviewQueue||[]),...lessonItems,...essaysNeedRewrite(d).map(e=>({id:'essay-'+e.id,errorType:'Essay rewrite',priority:'high',nextReviewDate:Store.today(),action:e.title||'Rewrite essay',status:'pending'})),...vocabItems].filter(x=>x.status!=='done');document.getElementById('view').innerHTML=`<div class="grid cols-3">${['high','medium','low'].map(pr=>`<section class="card"><h3>${pr.toUpperCase()} Priority</h3>${all.filter(x=>(x.priority||'medium')===pr).map(q=>`<div class="review-item"><div><b>${UI.esc(q.errorType)}</b><p>${UI.esc(q.action||Tutor.reviewTaskFor(q.errorType))}</p><p class="muted">Due: ${q.nextReviewDate||'today'} · Lesson: ${Tutor.lessonById(q.relatedLessonId)?.title||q.relatedLessonId||'—'}</p></div><button class="btn doneReview" data-id="${q.id}">Done</button></div>`).join('')||'<div class="empty">Không có item.</div>'}</section>`).join('')}</div>`;document.querySelectorAll('.doneReview').forEach(b=>b.onclick=()=>{Store.update(d=>{const item=d.reviewQueue.find(x=>x.id===b.dataset.id);if(item)item.status='done';return d});Tutor.addXP(6,'Review done');renderReviewQueue()})}
function renderVocabulary(){const d=Store.load();title('Vocabulary Notebook','Phrase, Vietnamese meaning, collocation, IELTS example, band level, mistake note.');const topics=IELTS_DATA.topics;document.getElementById('view').innerHTML=`<section class="card"><div class="filter-row"><select id="vocabTopic"><option value="">All topics</option>${UI.selectOptions(topics)}</select><select id="vocabStatus"><option value="">All status</option>${UI.selectOptions(['New','Learning','Mastered'])}</select></div></section><div id="vocabList" class="grid cols-3" style="margin-top:18px"></div>`;function draw(){const d=Store.load();vocabList.innerHTML=IELTS_DATA.vocabulary.filter(v=>(!vocabTopic.value||v.topic===vocabTopic.value)&&(!vocabStatus.value||((d.vocabulary[v.id]?.status||v.status||'New')===vocabStatus.value))).slice(0,90).map(v=>`<div class="card"><span class="pill blue">${UI.esc(v.topic)}</span><h3>${UI.esc(v.phrase||v.word)}</h3><p><b>Nghĩa:</b> ${UI.esc(v.meaningVi)}</p><p><b>Collocation:</b> ${UI.esc(v.collocation)}</p><p><b>Example:</b> ${UI.esc(v.example)}</p><p><b>Band:</b> ${UI.esc(v.bandLevel||'6.0-6.5')}</p><select data-vocabstatus="${v.id}">${UI.selectOptions(['New','Learning','Mastered'],d.vocabulary[v.id]?.status||v.status||'New')}</select><div class="mini-actions"><button class="btn saveVocab" data-id="${v.id}">Save</button><button class="btn ghost reviewVocab" data-id="${v.id}" data-word="${UI.attr(v.phrase||v.word)}">Add to Review Queue</button></div></div>`).join('')}vocabTopic.onchange=draw;vocabStatus.onchange=draw;draw();document.addEventListener('click',e=>{if(e.target.classList.contains('saveVocab')){const id=e.target.dataset.id;Store.update(d=>{d.vocabulary[id]={status:document.querySelector(`[data-vocabstatus="${id}"]`).value,updatedAt:Store.today()};return d});UI.toast('Saved vocab status')}if(e.target.classList.contains('reviewVocab'))Tutor.addReview({errorType:'Vocabulary review',priority:'low',action:'Review phrase: '+e.target.dataset.word})})}
function renderIdeaBank(){
  title('Idea Bank','Topic → common issue → main idea → supporting idea → example → useful vocabulary.');
  const ideas = Array.isArray(IELTS_DATA.ideaBank) ? IELTS_DATA.ideaBank : (Array.isArray(IELTS_DATA.ideas) ? IELTS_DATA.ideas : []);
  document.getElementById('view').innerHTML=`<section class="card"><div class="filter-row"><select id="ideaTopic"><option value="">All topics</option>${UI.selectOptions(IELTS_DATA.topics)}</select><input class="input" id="ideaSearch" placeholder="Search idea, issue, vocabulary..."></div><p class="muted">${ideas.length} IELTS idea items. Mỗi item là material để build main idea/support/example, không phải full essay.</p></section><div id="ideaList" class="grid cols-3" style="margin-top:18px"></div>`;
  const listEl=document.getElementById('ideaList'), topicEl=document.getElementById('ideaTopic'), searchEl=document.getElementById('ideaSearch');
  function draw(){
    const term=(searchEl.value||'').toLowerCase();
    const arr=ideas.filter(i=>(!topicEl.value||i.topic===topicEl.value)&&(!term||[i.topic,i.commonIssue,i.commonQuestion,i.mainIdea,i.supportingIdea,i.example,(i.usefulVocabulary||[]).join(' ')].join(' ').toLowerCase().includes(term))).slice(0,180);
    listEl.innerHTML=arr.map(i=>`<div class="card"><span class="pill purple">${UI.esc(i.topic)}</span><h3>${UI.esc(i.commonIssue||i.commonQuestion||'Idea')}</h3><p><b>Main idea:</b> ${UI.esc(i.mainIdea)}</p><p><b>Supporting idea:</b> ${UI.esc(i.supportingIdea)}</p><p><b>Example:</b> ${UI.esc(i.example)}</p><p><b>Vocabulary:</b> ${(i.usefulVocabulary||[]).map(UI.esc).join(', ')}</p></div>`).join('') || '<div class="empty">Không tìm thấy idea phù hợp.</div>';
  }
  topicEl.onchange=draw; searchEl.oninput=draw; draw();
}
function renderFeedback(){const d=Store.load();title('Feedback System','Lưu feedback theo 4 tiêu chí IELTS.');document.getElementById('view').innerHTML=`<section class="card"><h3>Feedback History</h3>${(d.feedback||[]).map(f=>`<div class="revision-card"><span class="pill">${f.date}</span><h3>${UI.esc(Tutor.lessonById(f.lessonId)?.title||f.lesson||'Feedback')}</h3><p><b>Strengths:</b> ${UI.esc(f.strengths||'')}</p><p><b>Weaknesses:</b> ${UI.esc(f.weaknesses||'')}</p><p>${UI.esc(f.teacherComment||'')}</p></div>`).join('')||'<div class="empty">Chưa có feedback.</div>'}</section>`}
function renderProfileBackup(){const d=Store.load(),p=d.studentProfile;title('Profile & Backup','Student Profile, Study Settings, Theme, Backup, Restore, Reset, Developer Mode.');const keys=['studentProfile','completedLessons','lessonStatus','lessonHistory','studySessions','homework','essays','revisionHistory','errors','reviewQueue','vocabulary','ideaBank','teacherNotes','feedback','bandHistory','customAssignments','studyBuddy','exerciseAnswers','exerciseStatus','customExercises','settings','badges','featureRequests'];document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><div class="profile-card"><div class="avatar">${UI.esc((p.name||'IA').slice(0,2).toUpperCase())}</div><div><h3>Student Profile</h3><p>Thông tin này điều khiển Learning Console.</p></div></div><div class="form-row"><input class="input" id="pfName" placeholder="Tên học viên" value="${UI.attr(p.name)}"><input class="input" id="pfCurrent" type="number" step="0.5" value="${p.currentBand}"></div><div class="form-row"><input class="input" id="pfTarget" type="number" step="0.5" value="${p.targetBand}"><input class="input" id="pfStart" type="date" value="${p.startDate}"></div><div class="form-row"><input class="input" id="pfExam" type="date" value="${p.expectedExamDate}"><input class="input" id="pfSessions" type="number" value="${p.sessionsPerWeek}"></div><div class="form-row"><input class="input" id="pfLessons" type="number" value="${p.dailyGoal.lessons}"><input class="input" id="pfMinutes" type="number" value="${p.dailyGoal.minutes}"></div><div class="form-row"><input class="input" id="pfExercises" type="number" value="${p.dailyGoal.exercises}"><input class="input" id="pfEssays" type="number" value="${p.dailyGoal.essaysPerWeek||1}"></div><button class="btn primary" id="saveProfile">Save Profile</button></section><section class="card"><h3>Backup</h3><button class="btn primary" id="exportData">Sao lưu dữ liệu học tập</button><label class="label">Khôi phục dữ liệu</label><textarea id="importBox" placeholder="Dán JSON backup vào đây"></textarea><div class="mini-actions"><button class="btn" id="importData">Khôi phục dữ liệu</button><button class="btn danger" id="resetData">Xóa toàn bộ tiến độ</button></div><hr><label class="check-item"><input type="checkbox" id="devMode" ${d.settings.developerMode?'checked':''}> Developer Mode</label><div id="devKeys" class="dev-only" style="display:${d.settings.developerMode?'block':'none'}"><h3>LocalStorage keys</h3><pre>${keys.join('\n')}</pre></div></section></div>`;saveProfile.onclick=()=>{Store.update(d=>{d.studentProfile={...d.studentProfile,name:pfName.value,currentBand:Number(pfCurrent.value)||5.5,targetBand:Number(pfTarget.value)||6.5,startDate:pfStart.value,expectedExamDate:pfExam.value,sessionsPerWeek:Number(pfSessions.value)||4,dailyGoal:{lessons:Number(pfLessons.value)||1,minutes:Number(pfMinutes.value)||45,exercises:Number(pfExercises.value)||5,essaysPerWeek:Number(pfEssays.value)||1},onboarded:true};return d});UI.toast('Profile saved')};exportData.onclick=()=>UI.modal('Sao lưu dữ liệu học tập',`<p>Copy JSON này và lưu vào máy.</p><pre>${UI.esc(Store.exportData())}</pre>`,`<button class="btn primary" id="copyBackup">Copy backup</button>`).querySelector('#copyBackup').onclick=()=>UI.copy(Store.exportData());importData.onclick=()=>{try{Store.importData(importBox.value);UI.toast('Đã khôi phục dữ liệu');location.reload()}catch(e){UI.toast('JSON không hợp lệ')}};resetData.onclick=()=>{if(confirm('Xóa toàn bộ tiến độ trong trình duyệt này?')){Store.reset();location.href='onboarding.html'}};devMode.onchange=()=>{Store.update(d=>{d.settings.developerMode=devMode.checked;return d});renderProfileBackup()}}
function renderChangelog(){const d=Store.load();title('Changelog + Feature Requests','v1.0 → v1.1 → v2.0 → v2.2.');document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Changelog</h3>${IELTS_DATA.changelog.map(c=>`<div class="changelog-item"><span class="pill purple">${c.version}</span><span class="pill">${c.date}</span><h3>${UI.esc(c.title)}</h3><ul>${c.details.map(x=>`<li>${UI.esc(x)}</li>`).join('')}</ul></div>`).join('')}</section><section class="card"><h3>Roadmap</h3><div class="timeline-v2"><div class="step"><b>v1.0 – Offline LMS</b><span>done</span></div><div class="step"><b>v1.1 – Learning Data + Teacher Workflow</b><span>done</span></div><div class="step"><b>v2.0 – Intelligent Tutor</b><span>done</span></div><div class="step"><b>v2.2 – Pedagogy + Exercise Bank + Coding UI</b><span>done</span></div><div class="step"><b>v2.2 – Teacher report export</b><span>idea</span></div></div><hr><h3>Feature Requests</h3><input class="input" id="frTitle" placeholder="Ý tưởng tính năng"><textarea id="frNote" placeholder="Mô tả"></textarea><button class="btn primary" id="addFr">Add Feature Request</button>${(d.featureRequests||[]).map(fr=>`<div class="review-item"><div><b>${UI.esc(fr.title)}</b><p>${UI.esc(fr.note||'')}</p></div><span class="pill">${UI.esc(fr.status||'idea')}</span></div>`).join('')}</section></div>`;addFr.onclick=()=>{Store.update(d=>{d.featureRequests.unshift({id:Store.uid('fr'),title:frTitle.value,status:'idea',note:frNote.value,date:Store.today()});return d});renderChangelog()}}
function renderOnboarding(){document.documentElement.dataset.theme=Store.load().settings.theme||'dark';document.body.dataset.theme=Store.load().settings.theme||'dark';const p=Store.load().studentProfile;document.getElementById('app').innerHTML=`<div class="onboarding"><section class="card hero"><span class="code-kicker">setup.profile()</span><h1>Welcome to IELTS Academy v4.0</h1><p>Thiết lập để Learning Console biết hôm nay em nên học gì.</p></section><section class="card" style="margin-top:18px"><div class="form-row"><input class="input" id="obName" placeholder="Tên em" value="${UI.attr(p.name)}"><input class="input" id="obCurrent" type="number" step="0.5" value="${p.currentBand}"></div><div class="form-row"><input class="input" id="obTarget" type="number" step="0.5" value="${p.targetBand}"><input class="input" id="obExam" type="date" value="${p.expectedExamDate}"></div><div class="form-row"><input class="input" id="obSessions" type="number" value="${p.sessionsPerWeek}"><input class="input" id="obMinutes" type="number" value="${p.dailyGoal.minutes}"></div><div class="form-row"><input class="input" id="obLessons" type="number" value="${p.dailyGoal.lessons}"><input class="input" id="obExercises" type="number" value="${p.dailyGoal.exercises}"></div><button class="btn primary glow" id="finishOnboarding">Start Learning →</button></section></div>`;finishOnboarding.onclick=()=>{Store.update(d=>{d.studentProfile={...d.studentProfile,name:obName.value||'Học viên IELTS',currentBand:Number(obCurrent.value)||5.5,targetBand:Number(obTarget.value)||6.5,expectedExamDate:obExam.value,sessionsPerWeek:Number(obSessions.value)||4,dailyGoal:{lessons:Number(obLessons.value)||1,minutes:Number(obMinutes.value)||45,exercises:Number(obExercises.value)||5,essaysPerWeek:1},onboarded:true};return d});location.href='dashboard.html'}}

/* ===== v2.2 Context Engine + Accounts + Admin Dashboard overrides ===== */
function rolePages(){const r=window.Accounts?Accounts.role():'student';return pages.filter(p=>p[0]!=='login' && (!p[4]||p[4].includes(r)))}
function renderNavGroup(group,page){const arr=rolePages().filter(p=>p[5]===group);return arr.length?`<div class="nav-section">${group}</div>${arr.map(p=>navLink(p,page)).join('')}`:''}
function layout(page){const data=Store.load(), user=Accounts.currentUser();document.documentElement.dataset.theme=data.settings.theme||'dark';document.body.dataset.theme=data.settings.theme||'dark';document.body.dataset.page=page;const app=document.getElementById('app');app.innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="brand-logo">IA</div><div><h1>IELTS Academy</h1><p>Instructional Design v3.0.2</p></div></div><div class="account-card"><div class="avatar small">${UI.esc((user.avatar||user.name||'IA').slice(0,2).toUpperCase())}</div><div><b>${UI.esc(user.name)}</b><p><span class="role-badge role-${UI.esc(user.role)}">${UI.esc(user.role)}</span> · ${UI.esc(user.username||user.id)}</p></div></div>${renderNavGroup('Console',page)}${renderNavGroup('Learn',page)}${renderNavGroup('Tutor Tools',page)}${renderNavGroup('Teacher',page)}${renderNavGroup('Admin',page)}${renderNavGroup('Account',page)}${renderNavGroup('System',page)}<div class="sidebar-mini"><strong>Progress pipeline</strong><p>Learn → Skill Practice → Homework → Teacher Feedback → Rewrite → Review → Mastered.</p><div class="progress"><span style="width:${Tutor.progress()}%"></span></div><p>${Tutor.progress()}% compiled · ${data.xp||0} XP · ${Tutor.masteryLevel()}</p><button class="btn ghost full" id="switchAccountBtn">Switch account</button></div></aside><main class="main"><div class="topbar"><div><h2 id="pageTitle"></h2><p id="pageSub"></p></div><div class="top-actions"><input class="search" id="globalSearch" placeholder="Search prompts, lessons, errors..."/><button class="btn ask-teacher glow" id="askGlobal">💬 Ask Chị</button><button class="btn ghost" id="themeBtn">🌗 Theme</button><a class="btn primary glow" href="${pageHref('dashboard.html')}" data-route="dashboard" data-file="dashboard.html">Run Today’s Lesson →</a></div></div><div id="view" class="page-transition"></div><div class="footer-note">IELTS Academy v4.0 offline demo. Dữ liệu được tách theo user trong browser localStorage. PIN chỉ là mô phỏng, không phải bảo mật thật.</div></main></div>`;document.getElementById('themeBtn').onclick=()=>{Store.update(d=>{d.settings.theme=d.settings.theme==='dark'?'light':'dark';return d});renderPage(page)};document.getElementById('switchAccountBtn').onclick=()=>location.href=pageHref('login.html');document.getElementById('askGlobal').onclick=()=>ContextEngine.ask({source:page,taskType:'General question from '+page,problem:'Em cần hỏi chị/gia sư về màn hình hiện tại.'},{level:'Standard'});document.getElementById('globalSearch').addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.value.trim())navigate('prompt-bank',`prompt-bank.html?q=${encodeURIComponent(e.target.value.trim())}`)});document.querySelectorAll('[data-route]').forEach(a=>a.addEventListener('click',e=>{if(e.button||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();navigate(a.dataset.route,a.getAttribute('href'))}))}
function mapRenderers(){return {dashboard:renderDashboard,roadmap:renderRoadmap,'chapter-review':renderChapterReview,lesson:renderLesson,'exercise-bank':renderExerciseBank,practice:renderPractice,homework:renderHomework,essays:renderEssays,'prompt-bank':renderPromptBank,'sample-database':renderSampleDatabase,'error-book':renderErrorLog,'review-queue':renderReviewQueue,vocabulary:renderVocabulary,'idea-bank':renderIdeaBank,'band-tracker':renderBandTracker,'teacher-mode':renderTeacherMode,'custom-assignment':renderCustomAssignment,'study-buddy':renderStudyBuddy,'teacher-notes':renderTeacherNotes,feedback:renderFeedback,'profile-backup':renderProfileBackup,changelog:renderChangelog,settings:renderProfileBackup,onboarding:renderOnboarding,login:renderLogin,'ai-context-builder':renderAIContextBuilder,'course-qa':renderCourseQA,'admin-dashboard':renderAdminDashboard,'teacher-dashboard':renderTeacherDashboard,'user-management':renderUserManagement,'course-builder':renderCourseBuilder,'student-review':renderTeacherDashboard}}
function renderPage(page){Store.touchStudy();Tutor.runAdaptiveRules();if(page==='onboarding'){renderOnboarding();return}if(page==='login'){renderLogin();return}layout(page);try{(mapRenderers()[page]||renderDashboard)()}catch(err){renderError(page,err)}}
function roleGuard(required){const r=Accounts.role();if(required==='admin'&&r!=='admin')return false;if(required==='teacher'&&!['teacher','admin'].includes(r))return false;return true}
function renderLogin(){document.documentElement.dataset.theme=Store.load().settings.theme||'dark';document.body.dataset.theme=Store.load().settings.theme||'dark';const sys=Accounts.readSystem(), users=sys.users.filter(u=>u.status!=='archived');document.getElementById('app').innerHTML=`<div class="login-shell"><section class="card hero login-card"><span class="code-kicker">auth.localStorage()</span><h1>IELTS Academy v4.0</h1><p>LMS + AI Tutor Platform. Đây là offline demo, dữ liệu lưu trong browser localStorage. Không dùng mật khẩu thật.</p><div class="privacy-box">PIN chỉ là mô phỏng để test LMS offline, không phải bảo mật thật.</div></section><section class="grid cols-2 login-grid"><div class="card"><h3>Login / Switch account</h3><label class="label">Chọn tài khoản</label><select id="loginUser">${users.map(u=>`<option value="${u.id}">${UI.esc(u.name)} · ${UI.esc(u.role)} · PIN ${UI.esc(u.pin)}</option>`).join('')}</select><input class="input" id="loginPin" placeholder="PIN" value="1234"><button class="btn primary glow full" id="loginBtn">Login →</button><p class="muted">Seed accounts: Phúc/1234, IELTS Teacher/2222, Admin/0000.</p></div><div class="card"><h3>Tạo account mới</h3><input class="input" id="newName" placeholder="Tên học viên / giáo viên" value="Phúc"><input class="input" id="newUsername" placeholder="username" value="phuc"><div class="form-row"><select id="newRole"><option>student</option><option>teacher</option><option>admin</option></select><input class="input" id="newPin" placeholder="PIN" value="1234"></div><div class="form-row"><input class="input" id="newCurrent" type="number" step="0.5" value="5.5"><input class="input" id="newTarget" type="number" step="0.5" value="6.5"></div><input class="input" id="newExam" type="date" value="${Store.addDays(90)}"><button class="btn full" id="createAccount">Create account</button></div></section></div>`;document.getElementById('loginUser').onchange=()=>{const u=users.find(x=>x.id===loginUser.value);loginPin.value=u?u.pin:''};document.getElementById('loginBtn').onclick=()=>{const res=Accounts.login(loginUser.value,loginPin.value);if(res.ok){UI.toast('Logged in as '+res.user.name);location.href='dashboard.html'}else UI.toast(res.message)};document.getElementById('createAccount').onclick=()=>{const user=Accounts.createUser({name:newName.value,username:newUsername.value,role:newRole.value,pin:newPin.value,currentBand:newCurrent.value,targetBand:newTarget.value,examDate:newExam.value});Accounts.switchUser(user.id);UI.toast('Created '+user.name);location.href=user.role==='student'?'onboarding.html':'dashboard.html'}}

function renderCourseQA(){const d=Store.load();title('Course QA Mode','Học viên đánh giá lesson thật để lần update sau ưu tiên sửa bài bị điểm thấp.');const rows=(d.courseQAFeedback||[]);const avg=(key)=>{const nums=rows.map(r=>Number(r[key])).filter(Boolean);return nums.length?(nums.reduce((a,b)=>a+b,0)/nums.length).toFixed(1):'—'};document.getElementById('view').innerHTML=`<section class="card hero"><span class="code-kicker">quality.assurance</span><h1>Course QA Mode</h1><p>Không chỉ tracking điểm số. Bản v3 tracking cả chất lượng sư phạm: dễ hiểu không, theory có khớp practice không, học viên có biết phải làm gì không.</p></section><div class="stat-strip"><div class="card metric"><span class="muted">Dễ hiểu</span><span class="metric-value">${avg('qa-understand')}</span></div><div class="card metric"><span class="muted">Theory ↔ Practice</span><span class="metric-value">${avg('qa-alignment')}</span></div><div class="card metric"><span class="muted">Rõ yêu cầu</span><span class="metric-value">${avg('qa-clarity')}</span></div><div class="card metric"><span class="muted">Biết làm gì</span><span class="metric-value">${avg('qa-action')}</span></div></div><section class="card" style="margin-top:18px"><h3>Lesson feedback list</h3>${rows.map(r=>{const l=Tutor.lessonById(r.lessonId)||{};return `<div class="review-item"><div><b>${UI.esc(l.chapterNo?l.chapterNo+'.'+l.lessonNo+' '+l.title:r.lessonTitle||r.lessonId)}</b><p>Understand ${r['qa-understand']||'—'}/5 · Alignment ${r['qa-alignment']||'—'}/5 · Clarity ${r['qa-clarity']||'—'}/5 · Action ${r['qa-action']||'—'}/5</p><p>${UI.esc(r['qa-feedback']||'')}</p></div><a class="btn" href="lesson.html?lesson=${r.lessonId}">Open</a></div>`}).join('')||'<div class="empty">Chưa có QA feedback. Mở một lesson → tab Course QA để đánh giá.</div>'}</section>`}

function renderAIContextBuilder(){const d=Store.load(), l=selectedLesson();title('AI Context Builder','Tạo prompt Quick / Standard / Full Context theo đúng nguồn dữ liệu.');document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Prompt config</h3><label class="label">Prompt level</label><select id="ctxLevel"><option>Quick</option><option selected>Standard</option><option>Full Context</option><option>Essay Review</option><option>Homework Review</option><option>Grammar Help</option><option>Vocabulary Help</option><option>Brainstorm Help</option></select><label class="label">Context source</label><select id="ctxSource"><option>lesson</option><option>homework</option><option>essay</option><option>error</option><option>vocabulary</option><option>study buddy</option></select><div class="grid cols-2"><label class="check-item"><input type="checkbox" id="incNotes" checked> teacher notes</label><label class="check-item"><input type="checkbox" id="incErrors" checked> error book</label><label class="check-item"><input type="checkbox" id="incBand" checked> band history</label><label class="check-item"><input type="checkbox" id="incQueue" checked> review queue</label></div><label class="label">Current task</label><input class="input" id="ctxTask" value="${UI.attr(l?l.title:'General question')}"><label class="label">My answer</label><textarea id="ctxAnswer" placeholder="Dán câu trả lời/bài làm ở đây nếu muốn."></textarea><label class="label">My problem</label><textarea id="ctxProblem" placeholder="Em đang kẹt ở đâu?"></textarea><div class="mini-actions"><button class="btn primary glow" id="genCtx">Generate Prompt</button><button class="btn" id="copyCtx">Copy Prompt</button><a class="btn" target="_blank" href="https://chatgpt.com/">Open ChatGPT</a></div></section><section class="card"><h3>Generated prompt</h3><textarea id="ctxOut" style="min-height:560px"></textarea></section></div>`;function gen(){ctxOut.value=ContextEngine.generatePrompt({source:ctxSource.value,level:ctxLevel.value,lessonId:l&&l.id,lesson:l&&l.title,skill:l&&(l.skillName||l.skill),taskType:ctxTask.value,answer:ctxAnswer.value,problem:ctxProblem.value},{includeTeacherNotes:incNotes.checked,includeErrorBook:incErrors.checked,includeBandHistory:incBand.checked,includeReviewQueue:incQueue.checked})}genCtx.onclick=gen;copyCtx.onclick=()=>UI.copy(ctxOut.value||ContextEngine.generatePrompt({source:ctxSource.value,level:ctxLevel.value,taskType:ctxTask.value,answer:ctxAnswer.value,problem:ctxProblem.value},{includeTeacherNotes:incNotes.checked,includeBandHistory:incBand.checked,includeReviewQueue:incQueue.checked}));gen()}
function adminBars(rows){const max=Math.max(1,...rows.map(r=>r.progress));return `<div class="chart-bars">${rows.map(r=>`<div class="bar-row"><span>${UI.esc(r.user.name)}</span><div class="bar-track"><div class="bar-fill" style="width:${UI.pct(r.progress,max)}%"></div></div><b>${r.progress}%</b></div>`).join('')}</div>`}
function errorTypeChart(rows){const m={};rows.forEach(r=>(r.data.errors||[]).forEach(e=>m[e.errorType]=(m[e.errorType]||0)+(Number(e.frequency)||1)));const arr=Object.entries(m).sort((a,b)=>b[1]-a[1]).slice(0,8);return arr.length?`<div class="chart-bars">${arr.map(([k,v])=>`<div class="bar-row"><span>${UI.esc(k)}</span><div class="bar-track"><div class="bar-fill" style="width:${UI.pct(v,arr[0][1])}%"></div></div><b>${v}</b></div>`).join('')}</div>`:'<div class="empty">Chưa có error data.</div>'}
function renderAdminDashboard(){if(!roleGuard('admin')){title('Admin Dashboard','Cần role admin.');document.getElementById('view').innerHTML='<section class="card warning-box"><h3>Access denied</h3><p>Đăng nhập tài khoản admin để xem dashboard này.</p></section>';return}const sum=Accounts.systemSummary();const q=new URLSearchParams(location.search), selected=q.get('student');const row=sum.rows.find(r=>r.user.id===selected);title('Admin Dashboard','Overview toàn hệ thống, student detail, data export và teacher workflow.');document.getElementById('view').innerHTML=`<div class="stat-strip"><div class="card metric"><span class="muted">Students</span><span class="metric-value">${sum.students.length}</span></div><div class="card metric"><span class="muted">Active</span><span class="metric-value">${sum.students.filter(u=>u.lastLogin).length}</span></div><div class="card metric"><span class="muted">Lessons done</span><span class="metric-value">${sum.totalLessons}</span></div><div class="card metric"><span class="muted">Essays</span><span class="metric-value">${sum.totalEssays}</span></div><div class="card metric"><span class="muted">Avg Band</span><span class="metric-value">${sum.avgBand.toFixed(1)}</span></div></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Progress by student</h3>${adminBars(sum.rows)}</section><section class="card"><h3>Errors by type</h3>${errorTypeChart(sum.rows)}</section></div><section class="card" style="margin-top:18px"><h3>Student List</h3><div class="table-wrap"><table><thead><tr><th>Student</th><th>Band</th><th>Progress</th><th>Last active</th><th>HW pending</th><th>Essay review</th><th>Main weakness</th><th></th></tr></thead><tbody>${sum.rows.map(r=>`<tr><td>${UI.esc(r.user.name)}</td><td>${r.user.currentBand} → ${r.user.targetBand}</td><td>${r.progress}%</td><td>${UI.esc(String(r.lastActive||'').slice(0,10))}</td><td>${r.pendingHw}</td><td>${r.pendingEssays}</td><td>${UI.esc(r.weakness)}</td><td><a class="btn" href="admin-dashboard.html?student=${r.user.id}">Open</a></td></tr>`).join('')}</tbody></table></div></section>${row?studentDetailCard(row):''}`;wireStudentDetail(row)}
function studentDetailCard(row){const d=row.data,u=row.user;return `<section class="card" style="margin-top:18px"><h3>Student Detail: ${UI.esc(u.name)}</h3><div class="grid cols-3"><div><b>Profile</b><p>${u.currentBand} → ${u.targetBand}<br>Exam: ${UI.esc(u.examDate||d.studentProfile.expectedExamDate||'')}</p></div><div><b>Current lesson</b><p>${UI.esc((nextLesson(d)||{}).title||'')}</p></div><div><b>Review queue</b><p>${(d.reviewQueue||[]).filter(q=>q.status!=='done').length} pending</p></div></div><div class="grid cols-2"><div><h4>Lesson History</h4>${(d.lessonHistory||[]).slice(0,5).map(h=>`<p>${UI.esc(h.lessonId)} · ${UI.esc(h.status)} · ${UI.esc((h.completedAt||h.startedAt||'').slice(0,10))}</p>`).join('')||'<div class="empty">No history</div>'}</div><div><h4>Error Log</h4>${(d.errors||[]).slice(0,5).map(e=>`<p>${UI.esc(e.errorType)} · ${e.frequency||1}x · ${UI.esc(e.status||'active')}</p>`).join('')||'<div class="empty">No errors</div>'}</div></div><hr><h3>Admin / Teacher Actions</h3><div class="form-row"><textarea id="adminNote" placeholder="Teacher note / weakness / next homework"></textarea><textarea id="adminFeedback" placeholder="Feedback or next steps"></textarea></div><div class="mini-actions"><button class="btn primary" id="addAdminNote" data-user="${u.id}">Add teacher note</button><button class="btn" id="unlockNext" data-user="${u.id}">Unlock next lesson</button><button class="btn" id="exportStudent" data-user="${u.id}">Export student data</button><button class="btn danger" id="resetStudent" data-user="${u.id}">Reset progress</button></div></section>`}
function wireStudentDetail(row){if(!row)return;const add=document.getElementById('addAdminNote');if(add)add.onclick=()=>{Accounts.updateUserData(add.dataset.user,d=>{d.teacherNotes.unshift({id:Store.uid('tn'),date:Store.today(),lessonId:(nextLesson(d)||{}).id,studentName:row.user.name,strengths:'',weaknesses:adminNote.value,nextHomework:adminFeedback.value,teacherComment:adminNote.value,status:'active',createdAt:Accounts.now(),updatedAt:Accounts.now()});return d});UI.toast('Teacher note added');renderAdminDashboard()};const unlock=document.getElementById('unlockNext');if(unlock)unlock.onclick=()=>{Accounts.updateUserData(unlock.dataset.user,d=>{const l=nextLesson(d);if(l)d.lessonStatus[l.id]='learn';return d});UI.toast('Lesson unlocked')};const exp=document.getElementById('exportStudent');if(exp)exp.onclick=()=>UI.modal('Export student data',`<pre>${UI.esc(Accounts.exportUser(exp.dataset.user))}</pre>`,`<button class="btn primary" id="copyStudentExport">Copy</button>`).querySelector('#copyStudentExport').onclick=()=>UI.copy(Accounts.exportUser(exp.dataset.user));const reset=document.getElementById('resetStudent');if(reset)reset.onclick=()=>{if(confirm('Reset progress học viên này?')){Accounts.saveUserData(reset.dataset.user,Accounts.LegacyStore.defaults);renderAdminDashboard()}}}
function renderTeacherDashboard(){if(!roleGuard('teacher')){title('Teacher Dashboard','Cần role teacher/admin.');document.getElementById('view').innerHTML='<section class="card warning-box"><h3>Access denied</h3><p>Đăng nhập tài khoản teacher hoặc admin để chữa bài.</p></section>';return}const sum=Accounts.systemSummary();let hw=[], essays=[], need=[];sum.rows.forEach(r=>{Object.entries(r.data.homework||{}).forEach(([lessonId,h])=>{if(['Submitted','Doing','Rewrite'].includes(h.status))hw.push({student:r.user,data:r.data,lessonId,h})});(r.data.essays||[]).forEach(e=>{if(e.status!=='final'&&e.status!=='mastered')essays.push({student:r.user,essay:e})});if((r.data.reviewQueue||[]).some(q=>q.status!=='done'))need.push(r)});title('Teacher Dashboard','Workflow chữa bài: Student Work → Feedback 4 criteria → Error tagging → Rewrite → Mark reviewed.');document.getElementById('view').innerHTML=`<div class="stat-strip"><div class="card metric"><span class="muted">Homework waiting</span><span class="metric-value">${hw.length}</span></div><div class="card metric"><span class="muted">Essays feedback</span><span class="metric-value">${essays.length}</span></div><div class="card metric"><span class="muted">Students need help</span><span class="metric-value">${need.length}</span></div><div class="card metric"><span class="muted">Overdue review</span><span class="metric-value">${sum.rows.reduce((s,r)=>s+(r.data.reviewQueue||[]).filter(q=>q.status!=='done'&&q.nextReviewDate<=Store.today()).length,0)}</span></div></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Homework waiting for review</h3>${hw.slice(0,12).map(x=>`<div class="review-item"><div><b>${UI.esc(x.student.name)} · ${UI.esc(Tutor.lessonById(x.lessonId)?.title||x.lessonId)}</b><p>${UI.esc(compact(x.h.answer||x.h.work||'',160))}</p></div><button class="btn openReview" data-user="${x.student.id}" data-lesson="${x.lessonId}">Review</button></div>`).join('')||'<div class="empty">No homework waiting.</div>'}</section><section class="card"><h3>Essays waiting for feedback</h3>${essays.slice(0,12).map(x=>`<div class="review-item"><div><b>${UI.esc(x.student.name)} · ${UI.esc(x.essay.title||'Essay')}</b><p>${UI.esc(compact(x.essay.prompt||x.essay.draft||'',160))}</p></div><button class="btn teacherPrompt" data-user="${x.student.id}" data-essay="${x.essay.id}">AI Review Prompt</button></div>`).join('')||'<div class="empty">No essays waiting.</div>'}</section></div><section class="card" style="margin-top:18px"><h3>Teacher Feedback Form</h3><div class="form-row"><select id="fbStudent">${sum.rows.map(r=>`<option value="${r.user.id}">${UI.esc(r.user.name)}</option>`).join('')}</select><select id="fbLesson">${IELTS_DATA.lessons.map(l=>`<option value="${l.id}">${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</option>`).join('')}</select></div><div class="grid cols-4"><input class="input" id="fbTR" placeholder="TR"><input class="input" id="fbCC" placeholder="CC"><input class="input" id="fbLR" placeholder="LR"><input class="input" id="fbGRA" placeholder="Grammar"></div><textarea id="fbStrengths" placeholder="strengths"></textarea><textarea id="fbWeaknesses" placeholder="weaknesses / corrected sentences / error tags"></textarea><textarea id="fbNext" placeholder="rewrite instruction + next homework"></textarea><div class="mini-actions"><button class="btn primary" id="saveTeacherFeedback">Save feedback + mark reviewed</button><button class="btn" id="genTeacherPrompt">Generate Teacher Review Prompt</button></div></section>`;document.querySelectorAll('.openReview').forEach(b=>b.onclick=()=>{fbStudent.value=b.dataset.user;fbLesson.value=b.dataset.lesson;UI.toast('Loaded student work into feedback form')});document.querySelectorAll('.teacherPrompt').forEach(b=>b.onclick=()=>{const d=Accounts.userData(b.dataset.user);const essay=(d.essays||[]).find(e=>e.id===b.dataset.essay)||{};UI.modal('Teacher Review Prompt',`<pre>${UI.esc(ContextEngine.teacherReviewPrompt(b.dataset.user,essay))}</pre>`,`<button class="btn primary" id="copyTRP">Copy</button>`).querySelector('#copyTRP').onclick=()=>UI.copy(ContextEngine.teacherReviewPrompt(b.dataset.user,essay))});saveTeacherFeedback.onclick=()=>{Accounts.updateUserData(fbStudent.value,d=>{d.feedback.unshift({id:Store.uid('fb'),userId:fbStudent.value,lessonId:fbLesson.value,date:Store.today(),taskResponse:fbTR.value,coherence:fbCC.value,lexicalResource:fbLR.value,grammar:fbGRA.value,strengths:fbStrengths.value,weaknesses:fbWeaknesses.value,nextHomework:fbNext.value,teacherComment:fbWeaknesses.value,status:'reviewed',createdAt:Accounts.now(),updatedAt:Accounts.now()});d.teacherNotes.unshift({id:Store.uid('tn'),date:Store.today(),lessonId:fbLesson.value,studentName:Accounts.readSystem().users.find(u=>u.id===fbStudent.value)?.name,strengths:fbStrengths.value,weaknesses:fbWeaknesses.value,nextHomework:fbNext.value,teacherComment:fbWeaknesses.value,status:'active'});if(d.homework[fbLesson.value])d.homework[fbLesson.value].status='Reviewed';d.lessonStatus[fbLesson.value]='rewrite';return d});UI.toast('Feedback saved');renderTeacherDashboard()};genTeacherPrompt.onclick=()=>{const d=Accounts.userData(fbStudent.value), h=(d.homework||{})[fbLesson.value]||{};UI.modal('Teacher Review Prompt',`<pre>${UI.esc(ContextEngine.teacherReviewPrompt(fbStudent.value,{lessonTitle:Tutor.lessonById(fbLesson.value)?.title,answer:h.answer||h.work||''}))}</pre>`,`<button class="btn primary" id="copyTeachP">Copy</button>`).querySelector('#copyTeachP').onclick=()=>UI.copy(ContextEngine.teacherReviewPrompt(fbStudent.value,{lessonTitle:Tutor.lessonById(fbLesson.value)?.title,answer:h.answer||h.work||''}))}}
function renderUserManagement(){if(!roleGuard('admin')){title('User Management','Cần role admin.');document.getElementById('view').innerHTML='<section class="card warning-box"><h3>Access denied</h3></section>';return}const users=Accounts.users();title('User Management','Tạo user, đổi role, reset PIN, archive, export/import user data.');document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Create user</h3><input class="input" id="umName" placeholder="Name"><input class="input" id="umUsername" placeholder="username"><div class="form-row"><select id="umRole"><option>student</option><option>teacher</option><option>admin</option></select><input class="input" id="umPin" value="1234" placeholder="PIN"></div><button class="btn primary" id="umCreate">Create user</button></section><section class="card"><h3>Whole system backup</h3><button class="btn primary" id="exportAll">Admin Export toàn hệ thống</button><textarea id="importAllBox" placeholder="Dán system JSON để import"></textarea><div class="mini-actions"><button class="btn" id="mergeAll">Import merge</button><button class="btn danger" id="replaceAll">Import replace</button></div></section></div><section class="card" style="margin-top:18px"><h3>Users</h3><div class="table-wrap"><table><thead><tr><th>Name</th><th>Username</th><th>Role</th><th>Status</th><th>PIN</th><th>Actions</th></tr></thead><tbody>${users.map(u=>`<tr><td>${UI.esc(u.name)}</td><td>${UI.esc(u.username)}</td><td><select data-role="${u.id}"><option ${u.role==='student'?'selected':''}>student</option><option ${u.role==='teacher'?'selected':''}>teacher</option><option ${u.role==='admin'?'selected':''}>admin</option></select></td><td>${UI.esc(u.status)}</td><td>${UI.esc(u.pin)}</td><td><button class="btn saveRole" data-id="${u.id}">Save</button><button class="btn resetPin" data-id="${u.id}">Reset PIN</button><button class="btn exportUser" data-id="${u.id}">Export</button><button class="btn danger archiveUser" data-id="${u.id}">Archive</button></td></tr>`).join('')}</tbody></table></div></section>`;umCreate.onclick=()=>{Accounts.createUser({name:umName.value,username:umUsername.value,role:umRole.value,pin:umPin.value});renderUserManagement()};document.querySelectorAll('.saveRole').forEach(b=>b.onclick=()=>{Accounts.updateUser(b.dataset.id,{role:document.querySelector(`[data-role="${b.dataset.id}"]`).value});renderUserManagement()});document.querySelectorAll('.resetPin').forEach(b=>b.onclick=()=>{Accounts.updateUser(b.dataset.id,{pin:'1234'});renderUserManagement()});document.querySelectorAll('.archiveUser').forEach(b=>b.onclick=()=>{Accounts.updateUser(b.dataset.id,{status:'archived'});renderUserManagement()});document.querySelectorAll('.exportUser').forEach(b=>b.onclick=()=>UI.modal('Export user data',`<pre>${UI.esc(Accounts.exportUser(b.dataset.id))}</pre>`,`<button class="btn primary" id="copyUserData">Copy</button>`).querySelector('#copyUserData').onclick=()=>UI.copy(Accounts.exportUser(b.dataset.id)));exportAll.onclick=()=>UI.modal('Admin Export toàn hệ thống',`<pre>${UI.esc(Accounts.exportSystem())}</pre>`,`<button class="btn primary" id="copyAllData">Copy</button>`).querySelector('#copyAllData').onclick=()=>UI.copy(Accounts.exportSystem());mergeAll.onclick=()=>{Accounts.importSystem(importAllBox.value,'merge');renderUserManagement()};replaceAll.onclick=()=>{if(confirm('Replace toàn bộ system data?')){Accounts.importSystem(importAllBox.value,'replace');renderUserManagement()}}}
function renderCourseBuilder(){if(!roleGuard('admin')){title('Course Builder','Cần role admin.');document.getElementById('view').innerHTML='<section class="card warning-box"><h3>Access denied</h3></section>';return}const sys=Accounts.readSystem();title('Course Builder','Thêm/sửa chapter, lesson, skill, practice, homework, prompt, vocabulary, idea bank bằng localStorage để sau này chuyển backend.');document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Add / edit lesson draft</h3><input class="input" id="cbTitle" placeholder="Lesson title"><div class="form-row"><input class="input" id="cbChapter" placeholder="Chapter" value="Custom"><input class="input" id="cbSkill" placeholder="Skill"></div><textarea id="cbTheory" placeholder="Theory in Vietnamese"></textarea><textarea id="cbPractice" placeholder="Skill Practice items, one per line"></textarea><textarea id="cbHomework" placeholder="Homework items, one per line"></textarea><button class="btn primary" id="saveLessonDraft">Save lesson draft</button></section><section class="card"><h3>Add prompt / vocabulary / idea</h3><textarea id="cbPrompt" placeholder="IELTS prompt question"></textarea><div class="form-row"><input class="input" id="cbType" placeholder="Essay type"><input class="input" id="cbTopic" placeholder="Topic"></div><button class="btn" id="savePromptDraft">Save prompt draft</button><hr><input class="input" id="cbVocab" placeholder="Word / phrase"><input class="input" id="cbMeaning" placeholder="Vietnamese meaning"><button class="btn" id="saveVocabDraft">Save vocabulary draft</button></section></div><section class="card" style="margin-top:18px"><h3>Drafts / backend-ready records</h3><p>System collections: users, courses, lessons, assignments, submissions, feedback, errors, bandHistory.</p><pre>${UI.esc(JSON.stringify({courses:sys.courses.length,lessons:sys.lessons.length,assignments:sys.assignments.length,submissions:sys.submissions.length,feedback:sys.feedback.length,errors:sys.errors.length,bandHistory:sys.bandHistory.length},null,2))}</pre></section>`;saveLessonDraft.onclick=()=>{Accounts.writeSystem((()=>{const s=Accounts.readSystem();s.lessons.unshift({id:Store.uid('lesson'),courseId:'course-task2-55-65',chapter:cbChapter.value,title:cbTitle.value,skill:cbSkill.value,theory:cbTheory.value,practice:cbPractice.value.split('\n').filter(Boolean),homework:cbHomework.value.split('\n').filter(Boolean),status:'draft',createdAt:Accounts.now(),updatedAt:Accounts.now()});return s})());UI.toast('Lesson draft saved')};savePromptDraft.onclick=()=>{Accounts.writeSystem((()=>{const s=Accounts.readSystem();s.assignments.unshift({id:Store.uid('prompt'),type:'prompt',question:cbPrompt.value,essayType:cbType.value,topic:cbTopic.value,status:'draft',createdAt:Accounts.now(),updatedAt:Accounts.now()});return s})());UI.toast('Prompt draft saved')};saveVocabDraft.onclick=()=>{Accounts.writeSystem((()=>{const s=Accounts.readSystem();s.assignments.unshift({id:Store.uid('vocab'),type:'vocabulary',phrase:cbVocab.value,meaning:cbMeaning.value,status:'draft',createdAt:Accounts.now(),updatedAt:Accounts.now()});return s})());UI.toast('Vocabulary draft saved')}}
function renderProfileBackup(){const d=Store.load(),p=d.studentProfile,user=Accounts.currentUser();title('Profile & Backup','Student Profile, Study Settings, Theme, Student Export, Admin Export, Restore, Developer Mode.');const keys=['users','courses','lessons','assignments','submissions','feedback','errors','bandHistory','studentProfile','completedLessons','lessonStatus','lessonHistory','studySessions','homework','essays','revisionHistory','reviewQueue','vocabulary','teacherNotes','customAssignments','settings'];document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><div class="profile-card"><div class="avatar">${UI.esc((user.avatar||p.name||'IA').slice(0,2).toUpperCase())}</div><div><h3>Student Profile</h3><p>${UI.esc(user.username)} · <span class="role-badge role-${UI.esc(user.role)}">${UI.esc(user.role)}</span></p></div></div><div class="form-row"><input class="input" id="pfName" placeholder="Tên học viên" value="${UI.attr(p.name)}"><input class="input" id="pfCurrent" type="number" step="0.5" value="${p.currentBand}"></div><div class="form-row"><input class="input" id="pfTarget" type="number" step="0.5" value="${p.targetBand}"><input class="input" id="pfExam" type="date" value="${p.expectedExamDate}"></div><div class="form-row"><input class="input" id="pfLessons" type="number" value="${p.dailyGoal.lessons}"><input class="input" id="pfMinutes" type="number" value="${p.dailyGoal.minutes}"></div><button class="btn primary" id="saveProfile">Save Profile</button><div class="privacy-box">This is an offline demo. Data is stored in your browser localStorage. Do not use real passwords. PIN chỉ là mô phỏng.</div></section><section class="card"><h3>Backup / Restore</h3><button class="btn primary" id="exportStudent">Student Export · Sao lưu dữ liệu học tập</button>${user.role==='admin'?'<button class="btn" id="exportSystem">Admin Export · toàn hệ thống</button>':''}<label class="label">Khôi phục dữ liệu</label><textarea id="importBox" placeholder="Dán JSON backup vào đây"></textarea><div class="mini-actions"><button class="btn" id="importCurrent">Import for current user</button><button class="btn" id="mergeSystem">Import merge data</button><button class="btn danger" id="resetData">Xóa tiến độ user hiện tại</button></div><hr><label class="check-item"><input type="checkbox" id="devMode" ${d.settings.developerMode?'checked':''}> Developer Mode</label><div id="devKeys" class="dev-only" style="display:${d.settings.developerMode?'block':'none'}"><h3>LocalStorage / backend-ready keys</h3><pre>${keys.join('\n')}</pre></div></section></div>`;saveProfile.onclick=()=>{Store.update(d=>{d.studentProfile={...d.studentProfile,name:pfName.value,currentBand:Number(pfCurrent.value)||5.5,targetBand:Number(pfTarget.value)||6.5,expectedExamDate:pfExam.value,dailyGoal:{...d.studentProfile.dailyGoal,lessons:Number(pfLessons.value)||1,minutes:Number(pfMinutes.value)||45},onboarded:true};return d});UI.toast('Profile saved')};exportStudent.onclick=()=>UI.modal('Student Export',`<pre>${UI.esc(Accounts.exportUser())}</pre>`,`<button class="btn primary" id="copyStudentExport2">Copy</button>`).querySelector('#copyStudentExport2').onclick=()=>UI.copy(Accounts.exportUser());const expSys=document.getElementById('exportSystem');if(expSys)expSys.onclick=()=>UI.modal('Admin Export',`<pre>${UI.esc(Accounts.exportSystem())}</pre>`,`<button class="btn primary" id="copySystemExport2">Copy</button>`).querySelector('#copySystemExport2').onclick=()=>UI.copy(Accounts.exportSystem());importCurrent.onclick=()=>{try{Accounts.importUser(importBox.value,'merge');UI.toast('Imported for current user');location.reload()}catch(e){UI.toast('JSON không hợp lệ')}};mergeSystem.onclick=()=>{try{Accounts.importSystem(importBox.value,'merge');UI.toast('Merged system data');location.reload()}catch(e){UI.toast('JSON không hợp lệ')}};resetData.onclick=()=>{if(confirm('Xóa tiến độ user hiện tại?')){Accounts.resetCurrent();location.reload()}};devMode.onchange=()=>{Store.update(d=>{d.settings.developerMode=devMode.checked;return d});renderProfileBackup()}}
function renderDashboard(){const d=Store.load(),p=d.studentProfile,nl=nextLesson(d),days=dateDiff(p.expectedExamDate),plan=todayPlan(d),total=plan.reduce((s,x)=>s+(x.time||0),0),week=Store.weeklyStats(),goal=p.dailyGoal||{},user=Accounts.currentUser();title('Learning Console',`${p.name||user.name} · Band ${p.currentBand} → ${p.targetBand} · ${user.role} mode`);document.getElementById('view').innerHTML=`<div class="dashboard-hero"><section class="card hero code-hero"><span class="code-kicker">// Today's Mission · ${UI.esc(user.name)}</span><h1>Run today’s IELTS pipeline — ${total} minutes</h1><div class="today-list">${plan.map((it,i)=>`<div class="today-task"><div class="task-num">${i+1}</div><div><b>${it.kind}: ${UI.esc(it.title)}</b><p>${UI.esc(it.desc)}</p></div><a class="btn" href="${it.href}">Open</a></div>`).join('')}</div><div class="mini-actions"><a class="btn primary glow" href="lesson.html?lesson=${nl.id}">Run Today’s Lesson →</a><button class="btn" id="startSession">Start Study Session</button><button class="btn ask-teacher glow" id="askDash">💬 Ask Chị</button></div></section><section class="card console-card"><h3>Exam Countdown</h3><div class="countdown">${days===null?'—':Math.max(0,days)}</div><p>ngày tới ngày thi dự kiến</p><div class="progress animated"><span style="width:${UI.pct(Math.max(0,90-(days||90)),90)}%"></span></div><hr><h3>Band Progress</h3><div class="metric-value">${Tutor.prediction()}</div><p>Target: ${p.targetBand}</p></section></div><div class="stat-strip"><div class="card metric"><span class="muted">XP</span><span class="metric-value">${d.xp||0}</span></div><div class="card metric"><span class="muted">Streak</span><span class="metric-value">${d.streak.count||0}🔥</span></div><div class="card metric"><span class="muted">Pipeline</span><span class="metric-value">${Tutor.progress()}%</span></div><div class="card metric"><span class="muted">Review</span><span class="metric-value">${dueReviews(d).length}</span></div><div class="card metric"><span class="muted">Rewrite</span><span class="metric-value">${essaysNeedRewrite(d).length}</span></div><div class="card metric"><span class="muted">Role</span><span class="metric-value role-mini">${UI.esc(user.role)}</span></div></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Weekly Goal</h3><p>${week.uniqueLessons}/${p.sessionsPerWeek||4} lessons · ${week.homework} homework · ${week.essays}/${goal.essaysPerWeek||1} essays · ${week.minutes} phút</p><div class="progress animated"><span style="width:${UI.pct(week.uniqueLessons,p.sessionsPerWeek||4)}%"></span></div><p class="muted">Goal mỗi ngày: ${goal.lessons||1} lesson · ${goal.minutes||45} phút · ${goal.exercises||5} micro exercises.</p></section><section class="card"><h3>Teacher Notes active</h3>${(d.teacherNotes||[]).filter(n=>n.status==='active').slice(0,3).map(n=>`<div class="review-item"><div><b>${UI.esc(Tutor.lessonById(n.lessonId)?.title||n.lessonId||'Note')}</b><p>${UI.esc(n.teacherComment||n.nextHomework||n.weaknesses||'')}</p></div><span class="pill purple">active</span></div>`).join('')||'<div class="empty">Chưa có teacher note active.</div>'}</section></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Error Log hôm nay</h3>${renderReviewMini(d)}</section><section class="card"><h3>Continue Pipeline</h3>${flowLine(currentStatus(nl.id,d))}<p><b>${nl.chapterNo}.${nl.lessonNo} ${UI.esc(nl.title)}</b></p><p>Skill hiện tại: ${UI.esc(nl.skillName||nl.skill)}. Không Mastered nếu chưa Skill Practice → Homework → Feedback → Rewrite/Reflection.</p></section></div><div class="grid cols-2" style="margin-top:18px"><section class="card"><h3>Band Tracker nhanh</h3>${bandChart(d.bandHistory,p)}</section><section class="card"><h3>Badges</h3>${badgeGrid(d)}</section></div>`;document.getElementById('startSession').onclick=startSessionModal;document.getElementById('askDash').onclick=()=>ContextEngine.ask({source:'dashboard',lessonId:nl.id,taskType:'Today’s Learning Plan',problem:'Em muốn hỏi hôm nay nên học gì và có được qua bài tiếp theo chưa.'},{level:'Standard'})}


/* ===== v3.0.2 Navigation Render Hotfix =====
   Những renderer này bị thiếu trong v3.0/v3.0.1. Vì mapRenderers tạo object literal với
   các identifier trực tiếp, chỉ cần thiếu 1 hàm là toàn bộ sidebar render fail. */
function renderBandTracker(){
  const d=Store.load(), p=d.studentProfile||{};
  const rows=(d.bandHistory||[]).slice().reverse();
  const latest=rows[0]||{};
  const scores=['taskResponse','coherence','lexicalResource','grammar'].map(k=>Number(latest[k]||latest[k.toUpperCase?.()]||0)).filter(Boolean);
  const bottleneck=scores.length?['Task Response','Coherence & Cohesion','Lexical Resource','Grammar'][scores.indexOf(Math.min(...scores))]:'Chưa có dữ liệu';
  title('Band Tracker','Theo dõi band theo thời gian, 4 tiêu chí IELTS và bottleneck kéo điểm xuống.');
  document.getElementById('view').innerHTML=`
    <div class="stat-strip">
      <div class="card metric"><span class="muted">Current estimate</span><span class="metric-value">${Number(latest.overallBand||latest.overall||p.currentBand||5.5).toFixed(1)}</span></div>
      <div class="card metric"><span class="muted">Target</span><span class="metric-value">${UI.esc(p.targetBand||6.5)}</span></div>
      <div class="card metric"><span class="muted">Entries</span><span class="metric-value">${rows.length}</span></div>
      <div class="card metric"><span class="muted">Main bottleneck</span><span class="metric-value small-text">${UI.esc(bottleneck)}</span></div>
    </div>
    <div class="grid cols-2" style="margin-top:18px">
      <section class="card"><h3>Band trend</h3>${bandChart(d.bandHistory,p)}</section>
      <section class="card"><h3>Criteria snapshot</h3>${criteriaBars(latest,p)}</section>
    </div>
    <section class="card" style="margin-top:18px">
      <h3>Add band estimate</h3>
      <div class="grid cols-4"><input class="input" id="btTR" placeholder="TR" type="number" step="0.5"><input class="input" id="btCC" placeholder="CC" type="number" step="0.5"><input class="input" id="btLR" placeholder="LR" type="number" step="0.5"><input class="input" id="btGRA" placeholder="GRA" type="number" step="0.5"></div>
      <textarea id="btComment" placeholder="Teacher comment / lý do band này"></textarea>
      <div class="mini-actions"><button class="btn primary" id="saveBandNow">Save Band</button><button class="btn ask-teacher glow" id="askBand">💬 Ask Chị</button></div>
    </section>
    <section class="card" style="margin-top:18px"><h3>History</h3>${rows.map(b=>`<div class="review-item"><div><b>${UI.esc(b.date||b.createdAt||'')}</b><p>TR ${b.taskResponse||'-'} · CC ${b.coherence||'-'} · LR ${b.lexicalResource||'-'} · GRA ${b.grammar||'-'} · Overall ${b.overallBand||b.overall||'-'}</p><p>${UI.esc(b.teacherComment||'')}</p></div><span class="pill purple">band</span></div>`).join('')||'<div class="empty">Chưa có band history. Sau mỗi feedback/essay, thêm band estimate tại đây.</div>'}</section>`;
  document.getElementById('saveBandNow').onclick=()=>{Store.update(d=>{const vals=[Number(btTR.value)||0,Number(btCC.value)||0,Number(btLR.value)||0,Number(btGRA.value)||0].filter(Boolean);const overall=vals.length?(vals.reduce((a,b)=>a+b,0)/vals.length).toFixed(1):'';d.bandHistory.unshift({id:Store.uid('band'),date:Store.today(),taskResponse:btTR.value,coherence:btCC.value,lexicalResource:btLR.value,grammar:btGRA.value,overallBand:overall,teacherComment:btComment.value,status:'active',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return d});UI.toast('Band saved');renderBandTracker()};
  document.getElementById('askBand').onclick=()=>ContextEngine.ask({source:'band-tracker',taskType:'Band progress review',problem:'Em muốn biết tiêu chí nào đang kéo band xuống và tuần này nên học gì.'},{level:'Full Context'});
}
function criteriaBars(latest,p){
  const base=Number(p.currentBand||5.5); const vals=[['Task Response',Number(latest.taskResponse||base)],['Coherence',Number(latest.coherence||base)],['Vocabulary',Number(latest.lexicalResource||base)],['Grammar',Number(latest.grammar||base)]];
  return `<div class="chart-bars">${vals.map(([k,v])=>`<div class="bar-row"><span>${UI.esc(k)}</span><div class="bar-track"><div class="bar-fill" style="width:${UI.pct(v,9)}%"></div></div><b>${v.toFixed(1)}</b></div>`).join('')}</div>`;
}
function renderTeacherMode(){
  if(!roleGuard('teacher')){title('Teacher Mode','Cần role teacher/admin.');document.getElementById('view').innerHTML='<section class="card warning-box"><h3>Access denied</h3><p>Đăng nhập teacher/admin để dùng Teacher Mode.</p></section>';return}
  const d=Store.load(); title('Teacher Mode','Công cụ chữa bài nhanh cho user hiện tại: note, feedback, unlock, reviewed/mastered.');
  document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Add Teacher Note</h3><select id="tmLesson">${IELTS_DATA.lessons.map(l=>`<option value="${l.id}">${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</option>`).join('')}</select><textarea id="tmStrengths" placeholder="Strengths"></textarea><textarea id="tmWeaknesses" placeholder="Weaknesses"></textarea><textarea id="tmNext" placeholder="Next focus / homework"></textarea><select id="tmPriority"><option>high</option><option selected>medium</option><option>low</option></select><button class="btn primary" id="tmSaveNote">Save note</button></section><section class="card"><h3>Lesson control</h3><select id="tmLesson2">${IELTS_DATA.lessons.map(l=>`<option value="${l.id}">${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</option>`).join('')}</select><div class="mini-actions"><button class="btn" id="tmUnlock">Unlock manually</button><button class="btn" id="tmReviewed">Mark Reviewed</button><button class="btn success" id="tmMastered">Mark Mastered</button></div><p class="muted">Teacher Mode dùng khi cần override vì học viên đã được chữa trực tiếp ngoài web.</p></section></div><section class="card" style="margin-top:18px"><h3>Active notes</h3>${(d.teacherNotes||[]).slice(0,20).map(n=>`<div class="review-item"><div><b>${UI.esc(Tutor.lessonById(n.lessonId)?.title||n.lessonId||'Note')}</b><p>${UI.esc(n.weaknesses||n.teacherComment||'')}</p><p class="muted">Next: ${UI.esc(n.nextHomework||n.nextFocus||'')}</p></div><span class="pill ${n.priority==='high'?'purple':'blue'}">${UI.esc(n.status||'active')}</span></div>`).join('')||'<div class="empty">Chưa có teacher note.</div>'}</section>`;
  tmSaveNote.onclick=()=>{Store.update(d=>{d.teacherNotes.unshift({id:Store.uid('tn'),date:Store.today(),lessonId:tmLesson.value,studentName:d.studentProfile.name,strengths:tmStrengths.value,weaknesses:tmWeaknesses.value,nextFocus:tmNext.value,nextHomework:tmNext.value,priority:tmPriority.value,teacherComment:tmWeaknesses.value,status:'active',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return d});UI.toast('Teacher note saved');renderTeacherMode()};
  tmUnlock.onclick=()=>{Store.update(d=>{d.lessonStatus[tmLesson2.value]='learn';return d});UI.toast('Lesson unlocked')};tmReviewed.onclick=()=>{Store.update(d=>{d.lessonStatus[tmLesson2.value]='reviewed';if(d.homework[tmLesson2.value])d.homework[tmLesson2.value].status='Reviewed';return d});UI.toast('Marked reviewed')};tmMastered.onclick=()=>{Store.update(d=>{d.lessonStatus[tmLesson2.value]='mastered';d.completedLessons[tmLesson2.value]=true;return d});Tutor.addXP(20,'Teacher mastered');UI.toast('Marked mastered')};
}
function renderCustomAssignment(){
  const d=Store.load(); title('Custom Assignment','Tạo bài tập thủ công khi cần thêm homework/rewrite ngoài course có sẵn.');
  document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Add Custom Exercise</h3><input class="input" id="caTitle" placeholder="Title" value="Topic sentence repair drill"><div class="form-row"><select id="caSkill">${UI.selectOptions(['brainstorm','paraphrase','thesis','topic sentence','body paragraph','conclusion','full essay','rewrite','grammar correction','vocabulary upgrade'])}</select><select id="caDifficulty">${UI.selectOptions(['easy','medium','hard'],'medium')}</select></div><textarea id="caPrompt" placeholder="IELTS context / question"></textarea><textarea id="caInstruction" placeholder="Instruction + requirement + checklist"></textarea><div class="form-row"><input class="input" id="caDue" type="date" value="${Store.addDays(3)}"><select id="caLesson">${IELTS_DATA.lessons.map(l=>`<option value="${l.id}">${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</option>`).join('')}</select></div><button class="btn primary glow" id="saveCustomAssignment">Save assignment</button></section><section class="card"><h3>Custom assignments</h3>${(d.customAssignments||[]).slice(0,25).map(a=>`<div class="review-item"><div><b>${UI.esc(a.title)}</b><p>${UI.esc(a.skill)} · due ${UI.esc(a.dueDate||'')}</p><p>${UI.esc(compact(a.instruction||a.prompt||'',180))}</p></div><span class="pill">${UI.esc(a.status||'assigned')}</span></div>`).join('')||'<div class="empty">Chưa có custom assignment.</div>'}</section></div>`;
  saveCustomAssignment.onclick=()=>{Store.update(d=>{d.customAssignments.unshift({id:Store.uid('assign'),title:caTitle.value,skill:caSkill.value,prompt:caPrompt.value,instruction:caInstruction.value,difficulty:caDifficulty.value,dueDate:caDue.value,relatedLesson:caLesson.value,status:'assigned',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return d});UI.toast('Custom assignment saved');renderCustomAssignment()};
}
function renderStudyBuddy(){
  const d=Store.load(), last=(d.studyBuddy||[])[0]||{}; title('Study Buddy','So sánh 2 câu trả lời: main ideas, supporting ideas, examples, vocabulary và feedback chung.');
  document.getElementById('view').innerHTML=`<div class="grid cols-2"><section class="card"><h3>Buddy Compare Lab</h3><textarea id="sbPrompt" placeholder="Prompt / context">${UI.esc(last.prompt||'')}</textarea><textarea id="sbA" placeholder="Student A answer">${UI.esc(last.studentA||'')}</textarea><textarea id="sbB" placeholder="Student B answer">${UI.esc(last.studentB||'')}</textarea><div class="grid cols-2"><textarea id="sbMain" placeholder="Compare main ideas">${UI.esc(last.compareMainIdeas||'')}</textarea><textarea id="sbSupport" placeholder="Compare supporting ideas">${UI.esc(last.compareSupportingIdeas||'')}</textarea></div><textarea id="sbFeedback" placeholder="Stronger answer / why / combined best version">${UI.esc(last.sharedFeedback||'')}</textarea><div class="mini-actions"><button class="btn primary" id="saveBuddy">Save comparison</button><button class="btn ask-teacher glow" id="askBuddy">💬 Ask Chị</button></div></section><section class="card"><h3>History</h3>${(d.studyBuddy||[]).slice(0,15).map(x=>`<div class="review-item"><div><b>${UI.esc(compact(x.prompt||'Buddy session',80))}</b><p>${UI.esc(compact(x.sharedFeedback||'',180))}</p></div><span class="pill">${UI.esc(x.date||'')}</span></div>`).join('')||'<div class="empty">Chưa có buổi Study Buddy.</div>'}</section></div>`;
  saveBuddy.onclick=()=>{Store.update(d=>{d.studyBuddy.unshift({id:Store.uid('buddy'),date:Store.today(),prompt:sbPrompt.value,studentA:sbA.value,studentB:sbB.value,compareMainIdeas:sbMain.value,compareSupportingIdeas:sbSupport.value,sharedFeedback:sbFeedback.value,status:'saved',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return d});UI.toast('Study Buddy saved');renderStudyBuddy()};
  askBuddy.onclick=()=>ContextEngine.ask({source:'study buddy',taskType:'Compare Student A and Student B',prompt:sbPrompt.value,answer:`Student A:\n${sbA.value}\n\nStudent B:\n${sbB.value}`,problem:'Em muốn chị so sánh ý nào mạnh hơn và vì sao.'},{level:'Standard'});
}
function renderTeacherNotes(){
  const d=Store.load(); title('Teacher Notes','Strengths → Weaknesses → Next Focus → Homework → Priority.');
  document.getElementById('view').innerHTML=`<section class="card"><h3>Add note</h3><select id="tnLesson">${IELTS_DATA.lessons.map(l=>`<option value="${l.id}">${l.chapterNo}.${l.lessonNo} ${UI.esc(l.title)}</option>`).join('')}</select><div class="form-row"><textarea id="tnStrengths" placeholder="Strengths"></textarea><textarea id="tnWeaknesses" placeholder="Weaknesses"></textarea></div><textarea id="tnNext" placeholder="Next focus / Homework"></textarea><div class="form-row"><select id="tnPriority"><option>high</option><option selected>medium</option><option>low</option></select><select id="tnStatus"><option selected>active</option><option>resolved</option></select></div><button class="btn primary" id="saveTeacherNote">Save Teacher Note</button></section><section class="card" style="margin-top:18px"><h3>Notes</h3>${(d.teacherNotes||[]).map(n=>`<div class="review-item"><div><b>${UI.esc(n.date||'')} · ${UI.esc(Tutor.lessonById(n.lessonId)?.title||n.lessonId||'General')}</b><p><b>Strengths:</b> ${UI.esc(n.strengths||'')}</p><p><b>Weaknesses:</b> ${UI.esc(n.weaknesses||n.teacherComment||'')}</p><p><b>Next:</b> ${UI.esc(n.nextFocus||n.nextHomework||'')}</p></div><span class="pill ${n.priority==='high'?'purple':'blue'}">${UI.esc(n.status||'active')}</span></div>`).join('')||'<div class="empty">Chưa có teacher notes.</div>'}</section>`;
  saveTeacherNote.onclick=()=>{Store.update(d=>{d.teacherNotes.unshift({id:Store.uid('tn'),date:Store.today(),lessonId:tnLesson.value,studentName:d.studentProfile.name,strengths:tnStrengths.value,weaknesses:tnWeaknesses.value,nextFocus:tnNext.value,nextHomework:tnNext.value,priority:tnPriority.value,teacherComment:tnWeaknesses.value,status:tnStatus.value,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});return d});UI.toast('Teacher note saved');renderTeacherNotes()};
}


function renderChapterReview(){
  title('Chapter Review','Summary → Mindmap → Common mistakes → Mini test → Reflection → AI Review.');
  const d=Store.load(), reviews=IELTS_DATA.chapterReviews||[];
  document.getElementById('view').innerHTML=`<section class="card"><h3>Chapter Review System</h3><p>Cuối mỗi chapter, học viên phải review trước khi unlock chapter tiếp theo. Đây là bước chống học mơ hồ.</p><div class="filter-row"><select id="crSelect">${reviews.map(r=>`<option value="${r.id}">${UI.esc(r.title)}</option>`).join('')}</select><button class="btn ask-teacher glow" id="askChapterReview">💬 Ask Chị review chapter</button></div></section><div id="chapterReviewView"></div>`;
  function draw(){const r=reviews.find(x=>x.id===crSelect.value)||reviews[0]; if(!r){chapterReviewView.innerHTML='<div class="empty">No chapter reviews.</div>';return} chapterReviewView.innerHTML=`<section class="card"><h2>${UI.esc(r.title)}</h2><h3>Summary</h3><p>${UI.esc(r.summary)}</p><h3>Mindmap</h3><div class="criteria-grid">${(r.mindmap||[]).map(m=>`<div class="criterion"><b>${UI.esc(m.node)}</b><br>→ ${UI.esc(m.connectsTo)}</div>`).join('')}</div><h3>Common mistakes</h3><ul>${(r.commonMistakes||[]).map(x=>`<li>${UI.esc(x)}</li>`).join('')}</ul><h3>Mini test</h3>${(r.miniTest||[]).map((ex,i)=>`<div class="homework-micro"><label class="label">${i+1}. ${UI.esc(ex.title||ex.task)}</label>${exerciseDetails(ex)}<textarea data-cr="${i}" placeholder="Làm mini test ở đây..."></textarea></div>`).join('')}<h3>Reflection</h3>${(r.reflection||[]).map((q,i)=>`<label class="label">${UI.esc(q)}</label><textarea data-cr-reflect="${i}"></textarea>`).join('')}<h3>AI Review Prompt</h3><p class="context-prompt">${UI.esc(r.aiReviewPrompt||'')}</p></section>`}
  crSelect.onchange=draw; draw(); askChapterReview.onclick=()=>{const r=reviews.find(x=>x.id===crSelect.value)||reviews[0];Tutor.ask({chapter:r?.title||'Chapter Review',lesson:'Chapter checkpoint',skill:'chapter review',taskType:'Chapter Review',prompt:r?.aiReviewPrompt||'',problem:'Em muốn chị đánh giá xem em đã đủ điều kiện qua chapter tiếp theo chưa.'},{level:'Full Context'})}
}
function renderSampleDatabase(){
  title('Sample Database','Band 9-style snippets by paragraph section. Dùng để phân tích, không copy nguyên bài.');
  const db=IELTS_DATA.sampleDatabase||[], topics=[...new Set(db.map(x=>x.topic))], types=[...new Set(db.map(x=>x.essayType))];
  document.getElementById('view').innerHTML=`<section class="card"><h3>Band 9-style Sample Database</h3><p>Mỗi item lưu snippet theo Introduction / Body 1 / Body 2 / Conclusion để lesson lấy đúng đoạn minh họa.</p><div class="filter-row"><select id="sdTopic"><option value="">All topics</option>${UI.selectOptions(topics)}</select><select id="sdType"><option value="">All essay types</option>${UI.selectOptions(types)}</select><select id="sdSection">${UI.selectOptions(['introduction','body1','body2','conclusion'],'body1')}</select><input class="input" id="sdSearch" placeholder="Search sample..."></div></section><div id="sampleList" class="grid cols-2" style="margin-top:18px"></div>`;
  function draw(){const term=(sdSearch.value||'').toLowerCase(), sec=sdSection.value;const arr=db.filter(x=>(!sdTopic.value||x.topic===sdTopic.value)&&(!sdType.value||x.essayType===sdType.value)&&(!term||JSON.stringify(x).toLowerCase().includes(term))).slice(0,80);sampleList.innerHTML=arr.map(x=>`<section class="card"><div class="lesson-meta"><span class="pill purple">${UI.esc(x.topic)}</span><span class="pill blue">${UI.esc(x.essayType)}</span><span class="pill">${UI.esc(sec)}</span></div><p class="context-prompt">${UI.esc(x[sec]||'')}</p><p class="muted">${UI.esc(x.notes||'')}</p><button class="btn ghost" onclick="navigator.clipboard&&navigator.clipboard.writeText('${UI.attr((x[sec]||'').replace(/'/g,'’'))}')">Copy snippet</button></section>`).join('')||'<div class="empty">No sample found.</div>'}
  [sdTopic,sdType,sdSection].forEach(e=>e.onchange=draw);sdSearch.oninput=draw;draw();
}

function compact(text,n=160){text=String(text||'');return text.length>n?text.slice(0,n)+'…':text}

})();
